# Difft Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Native macOS SwiftUI app to review GitHub PRs: pure-SwiftUI diff viewer, Claude clarification chat, agentic review + browser verification via `claude` CLI, self-contained HTML report.

**Architecture:** Swift Package with three library targets (`DifftCore` diff model/parsing, `DifftServices` gh/agent/report/session, `DifftUI` views) plus `DifftApp` executable target. All non-UI logic TDD'd via `swift test`. External processes (`gh`, `claude`) behind a `ProcessRunning` protocol for fake injection.

**Tech Stack:** Swift 5.10, SwiftUI, macOS 14+, Swift Package Manager, XCTest. Single third-party dependency: Highlightr (syntax highlighting). External CLIs at runtime: `gh`, `claude`, `git`.

**Spec:** `docs/superpowers/specs/2026-08-29-difft-design.md`

## Global Constraints

- macOS 14+, Swift 5.10, SPM only — no xcodeproj, no CocoaPods.
- Only third-party package dependency allowed: `Highlightr` (https://github.com/raspu/Highlightr, from 2.2.0).
- No WKWebView anywhere (spec decision: pure SwiftUI diff renderer).
- No OAuth/network code: all GitHub access through `gh` CLI subprocess; all Claude access through `claude` CLI subprocess.
- Session storage dir: `~/Library/Application Support/Difft/sessions/`; worktrees: `~/Library/Application Support/Difft/worktrees/<repo>-pr<N>`; reports: `~/Documents/Difft-reports/<repo>-pr<N>-<date>.html`.
- Verify task is the ONLY task allowed `--dangerously-skip-permissions`, and only after the per-run confirm dialog (Task 15).
- All processes parsed off the main actor; UI updates on main actor.
- Commit after every green test cycle. Commit messages: conventional (`feat:`, `test:`, `chore:`).

---

### Task 1: Package scaffold

**Files:**
- Create: `Package.swift`
- Create: `Sources/DifftCore/Placeholder.swift` (deleted in Task 2)
- Create: `Sources/DifftServices/Placeholder.swift` (deleted in Task 5)
- Create: `Sources/DifftUI/Placeholder.swift` (deleted in Task 11)
- Create: `Sources/DifftApp/main.swift` (replaced in Task 13)
- Create: `Tests/DifftCoreTests/SmokeTests.swift`
- Create: `.gitignore`

**Interfaces:**
- Consumes: nothing.
- Produces: buildable package; targets `DifftCore`, `DifftServices` (depends on DifftCore), `DifftUI` (depends on DifftCore, DifftServices, Highlightr), `DifftApp` (depends on all).

- [ ] **Step 1: Write Package.swift**

```swift
// swift-tools-version: 5.10
import PackageDescription

let package = Package(
    name: "Difft",
    platforms: [.macOS(.v14)],
    dependencies: [
        .package(url: "https://github.com/raspu/Highlightr", from: "2.2.0"),
    ],
    targets: [
        .target(name: "DifftCore"),
        .target(name: "DifftServices", dependencies: ["DifftCore"]),
        .target(name: "DifftUI", dependencies: ["DifftCore", "DifftServices", "Highlightr"]),
        .executableTarget(name: "DifftApp", dependencies: ["DifftCore", "DifftServices", "DifftUI"]),
        .testTarget(name: "DifftCoreTests", dependencies: ["DifftCore"]),
        .testTarget(name: "DifftServicesTests", dependencies: ["DifftServices"]),
    ]
)
```

- [ ] **Step 2: Write placeholder sources**

`Sources/DifftCore/Placeholder.swift`, `Sources/DifftServices/Placeholder.swift`, `Sources/DifftUI/Placeholder.swift` each:

```swift
// Placeholder so the target compiles; removed when real sources land.
```

`Sources/DifftApp/main.swift`:

```swift
print("Difft placeholder")
```

`.gitignore`:

```
.build/
.DS_Store
*.xcodeproj
```

- [ ] **Step 3: Write smoke test**

`Tests/DifftCoreTests/SmokeTests.swift`:

```swift
import XCTest

final class SmokeTests: XCTestCase {
    func testTruth() { XCTAssertTrue(true) }
}
```

- [ ] **Step 4: Build and test**

Run: `swift build && swift test`
Expected: build succeeds (Highlightr resolves), 1 test passes.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "chore: scaffold Difft swift package"
```

---

### Task 2: DiffCore model + unified diff parser

**Files:**
- Create: `Sources/DifftCore/DiffModel.swift`
- Create: `Sources/DifftCore/DiffParser.swift`
- Delete: `Sources/DifftCore/Placeholder.swift`
- Create: `Tests/DifftCoreTests/DiffParserTests.swift`
- Create: `Tests/DifftCoreTests/Fixtures.swift`

**Interfaces:**
- Consumes: nothing.
- Produces:
  - `enum LineKind: Equatable { case context, addition, deletion }`
  - `struct DiffLine: Equatable { let kind: LineKind; let oldNumber: Int?; let newNumber: Int?; let text: String }`
  - `struct Hunk: Equatable { let header: String; let lines: [DiffLine] }`
  - `enum FileChangeKind: Equatable { case modified, added, deleted, renamed(from: String), binary }`
  - `struct FileDiff: Equatable, Identifiable { var id: String { path }; let path: String; let kind: FileChangeKind; let hunks: [Hunk]; var additions: Int; var deletions: Int }`
  - `enum DiffParser { static func parse(_ text: String) -> [FileDiff] }`

- [ ] **Step 1: Write model**

`Sources/DifftCore/DiffModel.swift`:

```swift
public enum LineKind: Equatable, Sendable { case context, addition, deletion }

public struct DiffLine: Equatable, Sendable {
    public let kind: LineKind
    public let oldNumber: Int?
    public let newNumber: Int?
    public let text: String
    public init(kind: LineKind, oldNumber: Int?, newNumber: Int?, text: String) {
        self.kind = kind; self.oldNumber = oldNumber; self.newNumber = newNumber; self.text = text
    }
}

public struct Hunk: Equatable, Sendable {
    public let header: String
    public let lines: [DiffLine]
    public init(header: String, lines: [DiffLine]) { self.header = header; self.lines = lines }
}

public enum FileChangeKind: Equatable, Sendable {
    case modified, added, deleted, binary
    case renamed(from: String)
}

public struct FileDiff: Equatable, Identifiable, Sendable {
    public var id: String { path }
    public let path: String
    public let kind: FileChangeKind
    public let hunks: [Hunk]
    public var additions: Int { hunks.flatMap(\.lines).filter { $0.kind == .addition }.count }
    public var deletions: Int { hunks.flatMap(\.lines).filter { $0.kind == .deletion }.count }
    public init(path: String, kind: FileChangeKind, hunks: [Hunk]) {
        self.path = path; self.kind = kind; self.hunks = hunks
    }
}
```

- [ ] **Step 2: Write failing tests with real fixture strings**

`Tests/DifftCoreTests/Fixtures.swift`:

```swift
enum Fixtures {
    static let simpleModify = """
    diff --git a/src/app.py b/src/app.py
    index 1111111..2222222 100644
    --- a/src/app.py
    +++ b/src/app.py
    @@ -1,4 +1,4 @@ def main
     import os
    -DEBUG = False
    +DEBUG = True
     print(os.name)
    """

    static let addedFile = """
    diff --git a/new.txt b/new.txt
    new file mode 100644
    index 0000000..3333333
    --- /dev/null
    +++ b/new.txt
    @@ -0,0 +1,2 @@
    +hello
    +world
    """

    static let deletedFile = """
    diff --git a/gone.txt b/gone.txt
    deleted file mode 100644
    index 4444444..0000000
    --- a/gone.txt
    +++ /dev/null
    @@ -1,1 +0,0 @@
    -bye
    """

    static let binaryFile = """
    diff --git a/logo.png b/logo.png
    index 5555555..6666666 100644
    Binary files a/logo.png and b/logo.png differ
    """

    static let renamedFile = """
    diff --git a/old_name.swift b/new_name.swift
    similarity index 90%
    rename from old_name.swift
    rename to new_name.swift
    index 7777777..8888888 100644
    --- a/old_name.swift
    +++ b/new_name.swift
    @@ -3,3 +3,3 @@
     let a = 1
    -let b = 2
    +let b = 3
     let c = 4
    """

    static let multiHunk = """
    diff --git a/m.txt b/m.txt
    index aaaaaaa..bbbbbbb 100644
    --- a/m.txt
    +++ b/m.txt
    @@ -1,3 +1,3 @@
     one
    -two
    +TWO
     three
    @@ -10,3 +10,4 @@
     ten
     eleven
    +eleven.five
     twelve
    """
}
```

`Tests/DifftCoreTests/DiffParserTests.swift`:

```swift
import XCTest
@testable import DifftCore

final class DiffParserTests: XCTestCase {
    func testSimpleModify() {
        let files = DiffParser.parse(Fixtures.simpleModify)
        XCTAssertEqual(files.count, 1)
        let f = files[0]
        XCTAssertEqual(f.path, "src/app.py")
        XCTAssertEqual(f.kind, .modified)
        XCTAssertEqual(f.hunks.count, 1)
        XCTAssertEqual(f.hunks[0].header, "@@ -1,4 +1,4 @@ def main")
        let lines = f.hunks[0].lines
        XCTAssertEqual(lines.map(\.kind), [.context, .deletion, .addition, .context])
        XCTAssertEqual(lines[0].oldNumber, 1); XCTAssertEqual(lines[0].newNumber, 1)
        XCTAssertEqual(lines[1].oldNumber, 2); XCTAssertNil(lines[1].newNumber)
        XCTAssertEqual(lines[2].newNumber, 2); XCTAssertNil(lines[2].oldNumber)
        XCTAssertEqual(lines[3].oldNumber, 4); XCTAssertEqual(lines[3].newNumber, 4)
        XCTAssertEqual(lines[1].text, "DEBUG = False")
        XCTAssertEqual(f.additions, 1); XCTAssertEqual(f.deletions, 1)
    }

    func testAddedFile() {
        let f = DiffParser.parse(Fixtures.addedFile)[0]
        XCTAssertEqual(f.kind, .added)
        XCTAssertEqual(f.path, "new.txt")
        XCTAssertEqual(f.additions, 2)
    }

    func testDeletedFile() {
        let f = DiffParser.parse(Fixtures.deletedFile)[0]
        XCTAssertEqual(f.kind, .deleted)
        XCTAssertEqual(f.path, "gone.txt")
    }

    func testBinaryFile() {
        let f = DiffParser.parse(Fixtures.binaryFile)[0]
        XCTAssertEqual(f.kind, .binary)
        XCTAssertEqual(f.path, "logo.png")
        XCTAssertTrue(f.hunks.isEmpty)
    }

    func testRenamedFile() {
        let f = DiffParser.parse(Fixtures.renamedFile)[0]
        XCTAssertEqual(f.kind, .renamed(from: "old_name.swift"))
        XCTAssertEqual(f.path, "new_name.swift")
        XCTAssertEqual(f.hunks.count, 1)
    }

    func testMultiHunkNumbers() {
        let f = DiffParser.parse(Fixtures.multiHunk)[0]
        XCTAssertEqual(f.hunks.count, 2)
        let h2 = f.hunks[1].lines
        XCTAssertEqual(h2[0].oldNumber, 10)
        XCTAssertEqual(h2[2].kind, .addition)
        XCTAssertEqual(h2[2].newNumber, 12)
    }

    func testMultipleFilesAndGarbageTolerance() {
        let joined = Fixtures.simpleModify + "\n" + Fixtures.binaryFile + "\n" + Fixtures.addedFile
        XCTAssertEqual(DiffParser.parse(joined).count, 3)
        XCTAssertEqual(DiffParser.parse("").count, 0)
        XCTAssertEqual(DiffParser.parse("not a diff at all").count, 0)
    }
}
```

- [ ] **Step 3: Run tests, verify FAIL**

Run: `swift test --filter DiffParserTests`
Expected: compile error "cannot find 'DiffParser'".

- [ ] **Step 4: Implement parser**

`Sources/DifftCore/DiffParser.swift`:

```swift
public enum DiffParser {
    public static func parse(_ text: String) -> [FileDiff] {
        var files: [FileDiff] = []
        var lines = text.split(separator: "\n", omittingEmptySubsequences: false).map(String.init)[...]

        while let start = lines.firstIndex(where: { $0.hasPrefix("diff --git ") }) {
            lines = lines[start...]
            let header = lines.removeFirst()
            let next = lines.firstIndex(where: { $0.hasPrefix("diff --git ") }) ?? lines.endIndex
            let body = Array(lines[lines.startIndex..<next])
            lines = lines[next...]
            if let file = parseFile(headerLine: header, body: body) { files.append(file) }
        }
        return files
    }

    private static func parseFile(headerLine: String, body: [String]) -> FileDiff? {
        var kind: FileChangeKind = .modified
        var renameFrom: String?
        var path: String?

        for line in body {
            if line.hasPrefix("new file mode") { kind = .added }
            else if line.hasPrefix("deleted file mode") { kind = .deleted }
            else if line.hasPrefix("Binary files ") { kind = .binary }
            else if line.hasPrefix("rename from ") { renameFrom = String(line.dropFirst("rename from ".count)) }
            else if line.hasPrefix("rename to ") { path = String(line.dropFirst("rename to ".count)) }
            else if line.hasPrefix("+++ b/") { path = String(line.dropFirst("+++ b/".count)) }
            else if line.hasPrefix("--- a/"), kind == .deleted { path = String(line.dropFirst("--- a/".count)) }
        }
        if let from = renameFrom { kind = .renamed(from: from) }
        if path == nil {
            // binary or degenerate: take second path from "diff --git a/x b/x"
            let parts = headerLine.dropFirst("diff --git ".count).split(separator: " ")
            if let b = parts.last, b.hasPrefix("b/") { path = String(b.dropFirst(2)) }
        }
        guard let filePath = path else { return nil }
        return FileDiff(path: filePath, kind: kind, hunks: parseHunks(body))
    }

    private static func parseHunks(_ body: [String]) -> [Hunk] {
        var hunks: [Hunk] = []
        var header = ""
        var current: [DiffLine] = []
        var oldNo = 0, newNo = 0
        var inHunk = false

        func flush() {
            if inHunk { hunks.append(Hunk(header: header, lines: current)); current = [] }
        }

        for line in body {
            if line.hasPrefix("@@") {
                flush(); inHunk = true; header = line
                // "@@ -a,b +c,d @@ ..."
                let nums = line.split(separator: " ").dropFirst().prefix(2)
                if let o = nums.first { oldNo = Int(o.dropFirst().split(separator: ",")[0]) ?? 0 }
                if let n = nums.dropFirst().first { newNo = Int(n.dropFirst().split(separator: ",")[0]) ?? 0 }
            } else if inHunk {
                if line.hasPrefix("+") {
                    current.append(DiffLine(kind: .addition, oldNumber: nil, newNumber: newNo, text: String(line.dropFirst())))
                    newNo += 1
                } else if line.hasPrefix("-") {
                    current.append(DiffLine(kind: .deletion, oldNumber: oldNo, newNumber: nil, text: String(line.dropFirst())))
                    oldNo += 1
                } else if line.hasPrefix(" ") || line.isEmpty {
                    current.append(DiffLine(kind: .context, oldNumber: oldNo, newNumber: newNo, text: String(line.dropFirst(line.isEmpty ? 0 : 1))))
                    oldNo += 1; newNo += 1
                } else if line.hasPrefix("\\") {
                    continue // "\ No newline at end of file"
                } else {
                    inHunk = false; flush()
                }
            }
        }
        flush()
        return hunks
    }
}
```

Delete `Sources/DifftCore/Placeholder.swift`.

- [ ] **Step 5: Run tests, verify PASS, commit**

Run: `swift test --filter DiffParserTests`
Expected: all PASS. Fix parser (not tests) if not.

```bash
git add -A && git commit -m "feat: diff model and unified diff parser"
```

---

### Task 3: Side-by-side row pairing

**Files:**
- Create: `Sources/DifftCore/RowPairer.swift`
- Create: `Tests/DifftCoreTests/RowPairerTests.swift`

**Interfaces:**
- Consumes: `Hunk`, `DiffLine`, `LineKind` from Task 2.
- Produces:
  - `struct SideBySideRow: Equatable, Identifiable { let id: Int; let left: DiffLine?; let right: DiffLine? }`
  - `enum RowPairer { static func rows(for hunks: [Hunk]) -> [SideBySideRow] }` — context lines pair 1:1; consecutive deletion block + following addition block pair index-wise; overflow side gets `nil` on the other; ids are sequential over the whole file.

- [ ] **Step 1: Write failing tests**

`Tests/DifftCoreTests/RowPairerTests.swift`:

```swift
import XCTest
@testable import DifftCore

final class RowPairerTests: XCTestCase {
    private func line(_ kind: LineKind, _ text: String) -> DiffLine {
        DiffLine(kind: kind, oldNumber: kind == .addition ? nil : 1, newNumber: kind == .deletion ? nil : 1, text: text)
    }

    func testContextPairsBothSides() {
        let hunk = Hunk(header: "@@", lines: [line(.context, "a")])
        let rows = RowPairer.rows(for: [hunk])
        XCTAssertEqual(rows.count, 1)
        XCTAssertEqual(rows[0].left?.text, "a")
        XCTAssertEqual(rows[0].right?.text, "a")
    }

    func testBalancedChangeBlockPairsIndexWise() {
        let hunk = Hunk(header: "@@", lines: [
            line(.deletion, "d1"), line(.deletion, "d2"),
            line(.addition, "a1"), line(.addition, "a2"),
        ])
        let rows = RowPairer.rows(for: [hunk])
        XCTAssertEqual(rows.count, 2)
        XCTAssertEqual(rows[0].left?.text, "d1"); XCTAssertEqual(rows[0].right?.text, "a1")
        XCTAssertEqual(rows[1].left?.text, "d2"); XCTAssertEqual(rows[1].right?.text, "a2")
    }

    func testUnbalancedBlockFillsNil() {
        let hunk = Hunk(header: "@@", lines: [
            line(.deletion, "d1"),
            line(.addition, "a1"), line(.addition, "a2"), line(.addition, "a3"),
        ])
        let rows = RowPairer.rows(for: [hunk])
        XCTAssertEqual(rows.count, 3)
        XCTAssertEqual(rows[0].left?.text, "d1"); XCTAssertEqual(rows[0].right?.text, "a1")
        XCTAssertNil(rows[1].left); XCTAssertEqual(rows[1].right?.text, "a2")
        XCTAssertNil(rows[2].left); XCTAssertEqual(rows[2].right?.text, "a3")
    }

    func testAdditionOnlyBlock() {
        let hunk = Hunk(header: "@@", lines: [line(.context, "c"), line(.addition, "a1")])
        let rows = RowPairer.rows(for: [hunk])
        XCTAssertEqual(rows.count, 2)
        XCTAssertNil(rows[1].left)
        XCTAssertEqual(rows[1].right?.text, "a1")
    }

    func testIdsSequentialAcrossHunks() {
        let h1 = Hunk(header: "@@", lines: [line(.context, "x")])
        let h2 = Hunk(header: "@@", lines: [line(.context, "y")])
        let rows = RowPairer.rows(for: [h1, h2])
        XCTAssertEqual(rows.map(\.id), [0, 1])
    }
}
```

- [ ] **Step 2: Run tests, verify FAIL**

Run: `swift test --filter RowPairerTests`
Expected: compile error "cannot find 'RowPairer'".

- [ ] **Step 3: Implement**

`Sources/DifftCore/RowPairer.swift`:

```swift
public struct SideBySideRow: Equatable, Identifiable, Sendable {
    public let id: Int
    public let left: DiffLine?
    public let right: DiffLine?
    public init(id: Int, left: DiffLine?, right: DiffLine?) {
        self.id = id; self.left = left; self.right = right
    }
}

public enum RowPairer {
    public static func rows(for hunks: [Hunk]) -> [SideBySideRow] {
        var rows: [SideBySideRow] = []
        var nextID = 0
        func emit(_ l: DiffLine?, _ r: DiffLine?) {
            rows.append(SideBySideRow(id: nextID, left: l, right: r)); nextID += 1
        }

        for hunk in hunks {
            var i = 0
            let lines = hunk.lines
            while i < lines.count {
                let line = lines[i]
                switch line.kind {
                case .context:
                    emit(line, line); i += 1
                case .deletion, .addition:
                    var dels: [DiffLine] = [], adds: [DiffLine] = []
                    while i < lines.count, lines[i].kind == .deletion { dels.append(lines[i]); i += 1 }
                    while i < lines.count, lines[i].kind == .addition { adds.append(lines[i]); i += 1 }
                    for j in 0..<max(dels.count, adds.count) {
                        emit(j < dels.count ? dels[j] : nil, j < adds.count ? adds[j] : nil)
                    }
                }
            }
        }
        return rows
    }
}
```

- [ ] **Step 4: Run tests, verify PASS**

Run: `swift test --filter RowPairerTests`
Expected: all PASS.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: side-by-side row pairing"
```

---

### Task 4: Word-level intra-line diff

**Files:**
- Create: `Sources/DifftCore/IntralineDiff.swift`
- Create: `Tests/DifftCoreTests/IntralineDiffTests.swift`

**Interfaces:**
- Consumes: nothing (pure strings).
- Produces: `enum IntralineDiff { static func changedRanges(old: String, new: String) -> (old: [Range<Int>], new: [Range<Int>]) }` — ranges are **Character offsets** into each string (renderer converts to String.Index). Tokens = runs of alphanumerics vs. everything-else singletons; LCS over tokens; non-common tokens become ranges (adjacent ranges merged).

- [ ] **Step 1: Write failing tests**

`Tests/DifftCoreTests/IntralineDiffTests.swift`:

```swift
import XCTest
@testable import DifftCore

final class IntralineDiffTests: XCTestCase {
    func testSingleWordChange() {
        let r = IntralineDiff.changedRanges(old: "DEBUG = False", new: "DEBUG = True")
        XCTAssertEqual(r.old, [8..<13])   // "False"
        XCTAssertEqual(r.new, [8..<12])   // "True"
    }

    func testIdenticalStringsNoRanges() {
        let r = IntralineDiff.changedRanges(old: "same", new: "same")
        XCTAssertTrue(r.old.isEmpty); XCTAssertTrue(r.new.isEmpty)
    }

    func testCompletelyDifferent() {
        let r = IntralineDiff.changedRanges(old: "abc", new: "xyz")
        XCTAssertEqual(r.old, [0..<3]); XCTAssertEqual(r.new, [0..<3])
    }

    func testInsertionMergesAdjacent() {
        let r = IntralineDiff.changedRanges(old: "let a = 1", new: "let ab = 12")
        XCTAssertEqual(r.old, [4..<5, 8..<9])
        XCTAssertEqual(r.new, [4..<6, 10..<12])
    }

    func testEmptyOldSide() {
        let r = IntralineDiff.changedRanges(old: "", new: "new line")
        XCTAssertTrue(r.old.isEmpty)
        XCTAssertEqual(r.new, [0..<8])
    }
}
```

- [ ] **Step 2: Run tests, verify FAIL**

Run: `swift test --filter IntralineDiffTests`
Expected: compile error "cannot find 'IntralineDiff'".

- [ ] **Step 3: Implement**

`Sources/DifftCore/IntralineDiff.swift`:

```swift
public enum IntralineDiff {
    public static func changedRanges(old: String, new: String) -> (old: [Range<Int>], new: [Range<Int>]) {
        let oldTokens = tokenize(old), newTokens = tokenize(new)
        let common = lcs(oldTokens.map(\.text), newTokens.map(\.text))
        return (ranges(tokens: oldTokens, commonIndices: common.old),
                ranges(tokens: newTokens, commonIndices: common.new))
    }

    private struct Token { let text: String; let range: Range<Int> }

    private static func tokenize(_ s: String) -> [Token] {
        var tokens: [Token] = []
        var start = 0
        var currentIsWord: Bool?
        var buffer = ""
        for (i, ch) in s.enumerated() {
            let isWord = ch.isLetter || ch.isNumber || ch == "_"
            if let cw = currentIsWord, cw == isWord, isWord {
                buffer.append(ch)
            } else {
                if !buffer.isEmpty { tokens.append(Token(text: buffer, range: start..<i)) }
                buffer = String(ch); start = i
            }
            currentIsWord = isWord
        }
        if !buffer.isEmpty { tokens.append(Token(text: buffer, range: start..<s.count)) }
        return tokens
    }

    private static func lcs(_ a: [String], _ b: [String]) -> (old: Set<Int>, new: Set<Int>) {
        let n = a.count, m = b.count
        guard n > 0, m > 0 else { return ([], []) }
        var dp = Array(repeating: Array(repeating: 0, count: m + 1), count: n + 1)
        for i in 1...n { for j in 1...m {
            dp[i][j] = a[i-1] == b[j-1] ? dp[i-1][j-1] + 1 : max(dp[i-1][j], dp[i][j-1])
        }}
        var oldIdx = Set<Int>(), newIdx = Set<Int>()
        var i = n, j = m
        while i > 0, j > 0 {
            if a[i-1] == b[j-1] { oldIdx.insert(i-1); newIdx.insert(j-1); i -= 1; j -= 1 }
            else if dp[i-1][j] >= dp[i][j-1] { i -= 1 } else { j -= 1 }
        }
        return (oldIdx, newIdx)
    }

    private static func ranges(tokens: [Token], commonIndices: Set<Int>) -> [Range<Int>] {
        var out: [Range<Int>] = []
        for (i, tok) in tokens.enumerated() where !commonIndices.contains(i) {
            if let last = out.last, last.upperBound == tok.range.lowerBound {
                out[out.count - 1] = last.lowerBound..<tok.range.upperBound
            } else {
                out.append(tok.range)
            }
        }
        return out
    }
}
```

- [ ] **Step 4: Run tests, verify PASS**

Run: `swift test --filter IntralineDiffTests`
Expected: all PASS. If a range assertion fails by an off-by-one on token boundaries, fix tokenize/ranges, not the tests.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: word-level intraline diff"
```

---

### Task 5: ProcessRunning protocol + GitHubService

**Files:**
- Create: `Sources/DifftServices/ProcessRunning.swift`
- Create: `Sources/DifftServices/GitHubService.swift`
- Delete: `Sources/DifftServices/Placeholder.swift`
- Create: `Tests/DifftServicesTests/GitHubServiceTests.swift`

**Interfaces:**
- Consumes: `DiffParser`, `FileDiff` from DifftCore.
- Produces:
  - `struct ProcessResult: Equatable { let stdout: String; let stderr: String; let exitCode: Int32 }`
  - `protocol ProcessRunning: Sendable { func run(_ executable: String, arguments: [String], currentDirectory: URL?) async throws -> ProcessResult }`
  - `final class DefaultProcessRunner: ProcessRunning` (real `Foundation.Process`, resolves executables via `/usr/bin/env`)
  - `struct PullRequest: Codable, Equatable, Identifiable { var id: Int { number }; let number: Int; let title: String; let body: String; let headRefName: String; let authorLogin: String }`
  - `enum GitHubServiceError: Error, Equatable { case commandFailed(String) }`
  - `final class GitHubService { init(runner: ProcessRunning); func listPRs(repoDir: URL) async throws -> [PullRequest]; func fetchDiff(repoDir: URL, number: Int) async throws -> [FileDiff]; func checkAvailability() async -> (ghInstalled: Bool, ghAuthed: Bool) }`

- [ ] **Step 1: Write failing tests with FakeProcessRunner**

`Tests/DifftServicesTests/GitHubServiceTests.swift`:

```swift
import XCTest
@testable import DifftServices

final class FakeProcessRunner: ProcessRunning, @unchecked Sendable {
    var responses: [ProcessResult] = []
    var calls: [(executable: String, arguments: [String])] = []
    func run(_ executable: String, arguments: [String], currentDirectory: URL?) async throws -> ProcessResult {
        calls.append((executable, arguments))
        return responses.isEmpty ? ProcessResult(stdout: "", stderr: "", exitCode: 0) : responses.removeFirst()
    }
}

final class GitHubServiceTests: XCTestCase {
    func testListPRsParsesGhJSON() async throws {
        let fake = FakeProcessRunner()
        fake.responses = [ProcessResult(stdout: """
        [{"number": 12, "title": "Fix bug", "body": "Fixes crash", "headRefName": "fix/crash", "author": {"login": "alice"}}]
        """, stderr: "", exitCode: 0)]
        let svc = GitHubService(runner: fake)
        let prs = try await svc.listPRs(repoDir: URL(fileURLWithPath: "/tmp/repo"))
        XCTAssertEqual(prs, [PullRequest(number: 12, title: "Fix bug", body: "Fixes crash", headRefName: "fix/crash", authorLogin: "alice")])
        XCTAssertEqual(fake.calls[0].executable, "gh")
        XCTAssertEqual(fake.calls[0].arguments, ["pr", "list", "--json", "number,title,body,headRefName,author", "--limit", "50"])
    }

    func testFetchDiffParsesIntoFileDiffs() async throws {
        let fake = FakeProcessRunner()
        fake.responses = [ProcessResult(stdout: """
        diff --git a/a.txt b/a.txt
        index 1111111..2222222 100644
        --- a/a.txt
        +++ b/a.txt
        @@ -1,1 +1,1 @@
        -x
        +y
        """, stderr: "", exitCode: 0)]
        let svc = GitHubService(runner: fake)
        let files = try await svc.fetchDiff(repoDir: URL(fileURLWithPath: "/tmp/repo"), number: 12)
        XCTAssertEqual(files.count, 1)
        XCTAssertEqual(files[0].path, "a.txt")
        XCTAssertEqual(fake.calls[0].arguments, ["pr", "diff", "12"])
    }

    func testNonZeroExitThrows() async {
        let fake = FakeProcessRunner()
        fake.responses = [ProcessResult(stdout: "", stderr: "no auth", exitCode: 1)]
        let svc = GitHubService(runner: fake)
        do {
            _ = try await svc.listPRs(repoDir: URL(fileURLWithPath: "/tmp"))
            XCTFail("expected throw")
        } catch let e as GitHubServiceError {
            XCTAssertEqual(e, .commandFailed("no auth"))
        } catch { XCTFail("wrong error") }
    }
}
```

- [ ] **Step 2: Run tests, verify FAIL**

Run: `swift test --filter GitHubServiceTests`
Expected: compile errors for missing types.

- [ ] **Step 3: Implement**

`Sources/DifftServices/ProcessRunning.swift`:

```swift
import Foundation

public struct ProcessResult: Equatable, Sendable {
    public let stdout: String
    public let stderr: String
    public let exitCode: Int32
    public init(stdout: String, stderr: String, exitCode: Int32) {
        self.stdout = stdout; self.stderr = stderr; self.exitCode = exitCode
    }
}

public protocol ProcessRunning: Sendable {
    func run(_ executable: String, arguments: [String], currentDirectory: URL?) async throws -> ProcessResult
}

public final class DefaultProcessRunner: ProcessRunning {
    public init() {}
    public func run(_ executable: String, arguments: [String], currentDirectory: URL?) async throws -> ProcessResult {
        try await withCheckedThrowingContinuation { cont in
            let p = Process()
            p.executableURL = URL(fileURLWithPath: "/usr/bin/env")
            p.arguments = [executable] + arguments
            p.currentDirectoryURL = currentDirectory
            let out = Pipe(), err = Pipe()
            p.standardOutput = out; p.standardError = err
            p.terminationHandler = { proc in
                let stdout = String(data: out.fileHandleForReading.readDataToEndOfFile(), encoding: .utf8) ?? ""
                let stderr = String(data: err.fileHandleForReading.readDataToEndOfFile(), encoding: .utf8) ?? ""
                cont.resume(returning: ProcessResult(stdout: stdout, stderr: stderr, exitCode: proc.terminationStatus))
            }
            do { try p.run() } catch { cont.resume(throwing: error) }
        }
    }
}
```

`Sources/DifftServices/GitHubService.swift`:

```swift
import Foundation
import DifftCore

public struct PullRequest: Codable, Equatable, Identifiable, Sendable {
    public var id: Int { number }
    public let number: Int
    public let title: String
    public let body: String
    public let headRefName: String
    public let authorLogin: String
    public init(number: Int, title: String, body: String, headRefName: String, authorLogin: String) {
        self.number = number; self.title = title; self.body = body
        self.headRefName = headRefName; self.authorLogin = authorLogin
    }
}

public enum GitHubServiceError: Error, Equatable {
    case commandFailed(String)
}

public final class GitHubService: Sendable {
    private let runner: ProcessRunning
    public init(runner: ProcessRunning = DefaultProcessRunner()) { self.runner = runner }

    public func listPRs(repoDir: URL) async throws -> [PullRequest] {
        let r = try await runner.run("gh", arguments: ["pr", "list", "--json", "number,title,body,headRefName,author", "--limit", "50"], currentDirectory: repoDir)
        guard r.exitCode == 0 else { throw GitHubServiceError.commandFailed(r.stderr) }
        struct Raw: Codable { struct Author: Codable { let login: String }
            let number: Int; let title: String; let body: String; let headRefName: String; let author: Author }
        let raws = try JSONDecoder().decode([Raw].self, from: Data(r.stdout.utf8))
        return raws.map { PullRequest(number: $0.number, title: $0.title, body: $0.body, headRefName: $0.headRefName, authorLogin: $0.author.login) }
    }

    public func fetchDiff(repoDir: URL, number: Int) async throws -> [FileDiff] {
        let r = try await runner.run("gh", arguments: ["pr", "diff", String(number)], currentDirectory: repoDir)
        guard r.exitCode == 0 else { throw GitHubServiceError.commandFailed(r.stderr) }
        return DiffParser.parse(r.stdout)
    }

    public func checkAvailability() async -> (ghInstalled: Bool, ghAuthed: Bool) {
        guard let which = try? await runner.run("which", arguments: ["gh"], currentDirectory: nil), which.exitCode == 0 else {
            return (false, false)
        }
        let auth = try? await runner.run("gh", arguments: ["auth", "status"], currentDirectory: nil)
        return (true, (auth?.exitCode ?? 1) == 0)
    }
}
```

Delete `Sources/DifftServices/Placeholder.swift`.

- [ ] **Step 4: Run tests, verify PASS**

Run: `swift test --filter GitHubServiceTests`
Expected: all PASS.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: process runner and gh-backed GitHubService"
```

---

### Task 6: ReviewSession model + persistence

**Files:**
- Create: `Sources/DifftServices/ReviewSession.swift`
- Create: `Sources/DifftServices/SessionStore.swift`
- Create: `Tests/DifftServicesTests/SessionStoreTests.swift`

**Interfaces:**
- Consumes: `PullRequest` (Task 5), `Finding` (defined HERE, used by Task 8's parser and Task 10's report).
- Produces:
  - `struct Finding: Codable, Equatable, Sendable { let severity: String; let file: String; let line: Int; let explanation: String }`
  - `struct ChatMessage: Codable, Equatable, Sendable { let role: String; let text: String; let contextChip: String? }` (role is `"user"` or `"assistant"`)
  - `struct SessionData: Codable, Equatable, Sendable { var pr: PullRequest; var repoDir: String; var viewedFiles: Set<String>; var chat: [ChatMessage]; var findings: [Finding]; var verdict: String? }`
  - `final class SessionStore { init(directory: URL); func save(_ s: SessionData) throws; func load(repo: String, prNumber: Int) -> SessionData?; static func key(repo: String, prNumber: Int) -> String }` — corrupt JSON file is renamed `<name>.bak` and `load` returns nil.
  - `@MainActor final class ReviewSession: ObservableObject` — published mirrors of SessionData plus `selectedFile: String?`, `selectedLines: ClosedRange<Int>?`, `agentState: AgentState` (`enum AgentState: Equatable { case idle, running(String), failed(String) }`); `func snapshot() -> SessionData`; `func apply(_ data: SessionData)`.

- [ ] **Step 1: Write failing tests**

`Tests/DifftServicesTests/SessionStoreTests.swift`:

```swift
import XCTest
@testable import DifftServices

final class SessionStoreTests: XCTestCase {
    var dir: URL!
    override func setUp() {
        dir = FileManager.default.temporaryDirectory.appendingPathComponent(UUID().uuidString)
        try? FileManager.default.createDirectory(at: dir, withIntermediateDirectories: true)
    }
    override func tearDown() { try? FileManager.default.removeItem(at: dir) }

    private func sample() -> SessionData {
        SessionData(
            pr: PullRequest(number: 7, title: "T", body: "B", headRefName: "h", authorLogin: "a"),
            repoDir: "/tmp/repo", viewedFiles: ["a.txt"],
            chat: [ChatMessage(role: "user", text: "why?", contextChip: "a.txt:1-3")],
            findings: [Finding(severity: "high", file: "a.txt", line: 3, explanation: "bad")],
            verdict: nil)
    }

    func testRoundTrip() throws {
        let store = SessionStore(directory: dir)
        try store.save(sample())
        let loaded = store.load(repo: "repo", prNumber: 7)
        XCTAssertEqual(loaded, sample())
    }

    func testLoadMissingReturnsNil() {
        XCTAssertNil(SessionStore(directory: dir).load(repo: "nope", prNumber: 1))
    }

    func testCorruptFileRenamedToBak() throws {
        let store = SessionStore(directory: dir)
        let file = dir.appendingPathComponent(SessionStore.key(repo: "repo", prNumber: 7) + ".json")
        try Data("{{{not json".utf8).write(to: file)
        XCTAssertNil(store.load(repo: "repo", prNumber: 7))
        XCTAssertTrue(FileManager.default.fileExists(atPath: file.path + ".bak"))
        XCTAssertFalse(FileManager.default.fileExists(atPath: file.path))
    }
}
```

- [ ] **Step 2: Run tests, verify FAIL**

Run: `swift test --filter SessionStoreTests`
Expected: compile errors for missing types.

- [ ] **Step 3: Implement**

`Sources/DifftServices/ReviewSession.swift`:

```swift
import Foundation

public struct Finding: Codable, Equatable, Sendable {
    public let severity: String
    public let file: String
    public let line: Int
    public let explanation: String
    public init(severity: String, file: String, line: Int, explanation: String) {
        self.severity = severity; self.file = file; self.line = line; self.explanation = explanation
    }
}

public struct ChatMessage: Codable, Equatable, Sendable {
    public let role: String
    public let text: String
    public let contextChip: String?
    public init(role: String, text: String, contextChip: String?) {
        self.role = role; self.text = text; self.contextChip = contextChip
    }
}

public struct SessionData: Codable, Equatable, Sendable {
    public var pr: PullRequest
    public var repoDir: String
    public var viewedFiles: Set<String>
    public var chat: [ChatMessage]
    public var findings: [Finding]
    public var verdict: String?
    public init(pr: PullRequest, repoDir: String, viewedFiles: Set<String>, chat: [ChatMessage], findings: [Finding], verdict: String?) {
        self.pr = pr; self.repoDir = repoDir; self.viewedFiles = viewedFiles
        self.chat = chat; self.findings = findings; self.verdict = verdict
    }
}

public enum AgentState: Equatable, Sendable {
    case idle
    case running(String)
    case failed(String)
}

@MainActor
public final class ReviewSession: ObservableObject {
    @Published public var data: SessionData
    @Published public var selectedFile: String?
    @Published public var selectedLines: ClosedRange<Int>?
    @Published public var agentState: AgentState = .idle
    public init(data: SessionData) { self.data = data }
    public func snapshot() -> SessionData { data }
    public func apply(_ newData: SessionData) { data = newData }
}
```

`Sources/DifftServices/SessionStore.swift`:

```swift
import Foundation

public final class SessionStore {
    private let directory: URL
    public init(directory: URL) {
        self.directory = directory
        try? FileManager.default.createDirectory(at: directory, withIntermediateDirectories: true)
    }

    public static func key(repo: String, prNumber: Int) -> String { "\(repo)-pr\(prNumber)" }

    private func fileURL(repo: String, prNumber: Int) -> URL {
        directory.appendingPathComponent(Self.key(repo: repo, prNumber: prNumber) + ".json")
    }

    public func save(_ s: SessionData) throws {
        let repo = URL(fileURLWithPath: s.repoDir).lastPathComponent
        let data = try JSONEncoder().encode(s)
        try data.write(to: fileURL(repo: repo, prNumber: s.pr.number), options: .atomic)
    }

    public func load(repo: String, prNumber: Int) -> SessionData? {
        let url = fileURL(repo: repo, prNumber: prNumber)
        guard let data = try? Data(contentsOf: url) else { return nil }
        if let session = try? JSONDecoder().decode(SessionData.self, from: data) { return session }
        try? FileManager.default.moveItem(at: url, to: URL(fileURLWithPath: url.path + ".bak"))
        return nil
    }
}
```

- [ ] **Step 4: Run tests, verify PASS**

Run: `swift test --filter SessionStoreTests`
Expected: all PASS.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: review session model and persistent session store"
```

---

### Task 7: Agent stream-json parser

**Files:**
- Create: `Sources/DifftServices/AgentEvent.swift`
- Create: `Tests/DifftServicesTests/StreamJSONParserTests.swift`

**Interfaces:**
- Consumes: nothing.
- Produces:
  - `enum AgentEvent: Equatable, Sendable { case textDelta(String); case toolUse(name: String); case result(isError: Bool, text: String); case unknown(String) }`
  - `enum StreamJSONParser { static func event(fromLine: String) -> AgentEvent? }` — parses one NDJSON line of `claude -p --output-format stream-json --verbose` output. Returns nil for blank lines. Recognized shapes: `{"type":"assistant","message":{"content":[{"type":"text","text":"..."}]}}` → textDelta; content items `{"type":"tool_use","name":"Bash",...}` → toolUse; `{"type":"result","is_error":false,"result":"..."}` → result; anything else → unknown(rawLine).

- [ ] **Step 1: Write failing tests**

`Tests/DifftServicesTests/StreamJSONParserTests.swift`:

```swift
import XCTest
@testable import DifftServices

final class StreamJSONParserTests: XCTestCase {
    func testAssistantText() {
        let line = #"{"type":"assistant","message":{"content":[{"type":"text","text":"Hello"}]}}"#
        XCTAssertEqual(StreamJSONParser.event(fromLine: line), .textDelta("Hello"))
    }

    func testToolUse() {
        let line = #"{"type":"assistant","message":{"content":[{"type":"tool_use","name":"Bash","id":"t1","input":{}}]}}"#
        XCTAssertEqual(StreamJSONParser.event(fromLine: line), .toolUse(name: "Bash"))
    }

    func testResultSuccess() {
        let line = #"{"type":"result","subtype":"success","is_error":false,"result":"All good"}"#
        XCTAssertEqual(StreamJSONParser.event(fromLine: line), .result(isError: false, text: "All good"))
    }

    func testResultError() {
        let line = #"{"type":"result","subtype":"error_during_execution","is_error":true,"result":"boom"}"#
        XCTAssertEqual(StreamJSONParser.event(fromLine: line), .result(isError: true, text: "boom"))
    }

    func testBlankLineNil() {
        XCTAssertNil(StreamJSONParser.event(fromLine: "  "))
    }

    func testGarbageIsUnknown() {
        XCTAssertEqual(StreamJSONParser.event(fromLine: "not json"), .unknown("not json"))
    }

    func testSystemInitIsUnknown() {
        let line = #"{"type":"system","subtype":"init","session_id":"abc"}"#
        XCTAssertEqual(StreamJSONParser.event(fromLine: line), .unknown(line))
    }
}
```

- [ ] **Step 2: Run tests, verify FAIL**

Run: `swift test --filter StreamJSONParserTests`
Expected: compile error "cannot find 'StreamJSONParser'".

- [ ] **Step 3: Implement**

`Sources/DifftServices/AgentEvent.swift`:

```swift
import Foundation

public enum AgentEvent: Equatable, Sendable {
    case textDelta(String)
    case toolUse(name: String)
    case result(isError: Bool, text: String)
    case unknown(String)
}

public enum StreamJSONParser {
    public static func event(fromLine line: String) -> AgentEvent? {
        let trimmed = line.trimmingCharacters(in: .whitespaces)
        guard !trimmed.isEmpty else { return nil }
        guard let obj = try? JSONSerialization.jsonObject(with: Data(trimmed.utf8)) as? [String: Any],
              let type = obj["type"] as? String else {
            return .unknown(trimmed)
        }
        switch type {
        case "assistant":
            guard let message = obj["message"] as? [String: Any],
                  let content = message["content"] as? [[String: Any]],
                  let first = content.first, let itemType = first["type"] as? String else {
                return .unknown(trimmed)
            }
            if itemType == "text", let text = first["text"] as? String { return .textDelta(text) }
            if itemType == "tool_use", let name = first["name"] as? String { return .toolUse(name: name) }
            return .unknown(trimmed)
        case "result":
            let isError = obj["is_error"] as? Bool ?? false
            let text = obj["result"] as? String ?? ""
            return .result(isError: isError, text: text)
        default:
            return .unknown(trimmed)
        }
    }
}
```

- [ ] **Step 4: Run tests, verify PASS**

Run: `swift test --filter StreamJSONParserTests`
Expected: all PASS.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: claude stream-json event parser"
```

---

### Task 8: AgentService — tasks, prompts, findings parsing, process streaming

**Files:**
- Create: `Sources/DifftServices/AgentTask.swift`
- Create: `Sources/DifftServices/FindingsParser.swift`
- Create: `Sources/DifftServices/AgentService.swift`
- Create: `Tests/DifftServicesTests/AgentTaskTests.swift`
- Create: `Tests/DifftServicesTests/FindingsParserTests.swift`
- Create: `Tests/DifftServicesTests/AgentServiceTests.swift`

**Interfaces:**
- Consumes: `AgentEvent`, `StreamJSONParser` (Task 7), `Finding` (Task 6), `PullRequest` (Task 5).
- Produces:
  - `enum AgentTask { case clarify(pr: PullRequest, selection: String?, question: String, history: [ChatMessage]); case review(pr: PullRequest, diffSummary: String); case verify(pr: PullRequest) }` with `var prompt: String` and `var cliArguments: [String]` (clarify/review: `["-p", prompt, "--output-format", "stream-json", "--verbose", "--allowedTools", "Read,Grep,Glob"]`; verify: `["-p", prompt, "--output-format", "stream-json", "--verbose", "--dangerously-skip-permissions"]`).
  - `enum FindingsParser { static func parse(_ finalText: String) -> [Finding] }` — extracts first ```` ```json ```` fenced block (or whole text if it decodes) as `[{"severity","file","line","explanation"}]`.
  - `protocol StreamingProcessRunning: Sendable { func stream(_ executable: String, arguments: [String], currentDirectory: URL?) -> AsyncThrowingStream<String, Error>; func cancel() }` — yields stdout lines.
  - `final class DefaultStreamingProcessRunner: StreamingProcessRunning` (real Process + FileHandle readabilityHandler; `cancel()` sends SIGTERM).
  - `final class AgentService { init(runner: StreamingProcessRunning); func run(_ task: AgentTask, in worktree: URL) -> AsyncThrowingStream<AgentEvent, Error>; func cancel() }` — maps stdout lines through `StreamJSONParser`, drops nil.

- [ ] **Step 1: Write failing prompt/args tests**

`Tests/DifftServicesTests/AgentTaskTests.swift`:

```swift
import XCTest
@testable import DifftServices

final class AgentTaskTests: XCTestCase {
    let pr = PullRequest(number: 5, title: "Add login", body: "Adds login page", headRefName: "feat/login", authorLogin: "bob")

    func testClarifyPromptContainsAllContext() {
        let task = AgentTask.clarify(pr: pr, selection: "src/a.swift:10-12\n+let x = 1",
                                     question: "why x?",
                                     history: [ChatMessage(role: "user", text: "earlier q", contextChip: nil)])
        let p = task.prompt
        XCTAssertTrue(p.contains("Add login"))
        XCTAssertTrue(p.contains("src/a.swift:10-12"))
        XCTAssertTrue(p.contains("why x?"))
        XCTAssertTrue(p.contains("earlier q"))
    }

    func testClarifyArgsAreReadOnly() {
        let task = AgentTask.clarify(pr: pr, selection: nil, question: "q", history: [])
        let args = task.cliArguments
        XCTAssertEqual(args.first, "-p")
        XCTAssertTrue(args.contains("--allowedTools"))
        XCTAssertTrue(args.contains("Read,Grep,Glob"))
        XCTAssertFalse(args.contains("--dangerously-skip-permissions"))
    }

    func testReviewPromptAsksForJSONFindings() {
        let task = AgentTask.review(pr: pr, diffSummary: "3 files changed")
        XCTAssertTrue(task.prompt.contains("json"))
        XCTAssertTrue(task.prompt.contains("severity"))
        XCTAssertTrue(task.cliArguments.contains("Read,Grep,Glob"))
    }

    func testVerifyArgsSkipPermissions() {
        let task = AgentTask.verify(pr: pr)
        XCTAssertTrue(task.cliArguments.contains("--dangerously-skip-permissions"))
        XCTAssertTrue(task.prompt.contains("difft-evidence"))
        XCTAssertTrue(task.prompt.contains("Add login"))
    }
}
```

`Tests/DifftServicesTests/FindingsParserTests.swift`:

```swift
import XCTest
@testable import DifftServices

final class FindingsParserTests: XCTestCase {
    func testParsesFencedJSON() {
        let text = """
        Here is my review.
        ```json
        [{"severity": "high", "file": "a.swift", "line": 10, "explanation": "force unwrap"}]
        ```
        Done.
        """
        XCTAssertEqual(FindingsParser.parse(text),
                       [Finding(severity: "high", file: "a.swift", line: 10, explanation: "force unwrap")])
    }

    func testParsesBareJSON() {
        let text = #"[{"severity": "low", "file": "b.py", "line": 2, "explanation": "nit"}]"#
        XCTAssertEqual(FindingsParser.parse(text).count, 1)
    }

    func testMalformedReturnsEmpty() {
        XCTAssertEqual(FindingsParser.parse("no json here"), [])
        XCTAssertEqual(FindingsParser.parse("```json\nbroken\n```"), [])
    }
}
```

`Tests/DifftServicesTests/AgentServiceTests.swift`:

```swift
import XCTest
@testable import DifftServices

final class FakeStreamingRunner: StreamingProcessRunning, @unchecked Sendable {
    var lines: [String] = []
    var cancelled = false
    var lastArguments: [String] = []
    func stream(_ executable: String, arguments: [String], currentDirectory: URL?) -> AsyncThrowingStream<String, Error> {
        lastArguments = arguments
        let toYield = lines
        return AsyncThrowingStream { cont in
            for l in toYield { cont.yield(l) }
            cont.finish()
        }
    }
    func cancel() { cancelled = true }
}

final class AgentServiceTests: XCTestCase {
    let pr = PullRequest(number: 1, title: "t", body: "b", headRefName: "h", authorLogin: "a")

    func testMapsLinesToEvents() async throws {
        let fake = FakeStreamingRunner()
        fake.lines = [
            #"{"type":"assistant","message":{"content":[{"type":"text","text":"hi"}]}}"#,
            "",
            #"{"type":"result","is_error":false,"result":"done"}"#,
        ]
        let svc = AgentService(runner: fake)
        var events: [AgentEvent] = []
        for try await e in svc.run(.clarify(pr: pr, selection: nil, question: "q", history: []), in: URL(fileURLWithPath: "/tmp")) {
            events.append(e)
        }
        XCTAssertEqual(events, [.textDelta("hi"), .result(isError: false, text: "done")])
        XCTAssertEqual(fake.lastArguments.first, "-p")
    }

    func testCancelForwards() {
        let fake = FakeStreamingRunner()
        let svc = AgentService(runner: fake)
        svc.cancel()
        XCTAssertTrue(fake.cancelled)
    }
}
```

- [ ] **Step 2: Run tests, verify FAIL**

Run: `swift test --filter 'AgentTaskTests|FindingsParserTests|AgentServiceTests'`
Expected: compile errors for missing types.

- [ ] **Step 3: Implement**

`Sources/DifftServices/AgentTask.swift`:

```swift
import Foundation

public enum AgentTask: Sendable {
    case clarify(pr: PullRequest, selection: String?, question: String, history: [ChatMessage])
    case review(pr: PullRequest, diffSummary: String)
    case verify(pr: PullRequest)

    public var prompt: String {
        switch self {
        case let .clarify(pr, selection, question, history):
            var p = """
            You are helping review GitHub PR #\(pr.number): \(pr.title)

            PR description:
            \(pr.body)

            You are inside a checkout of the PR branch. You may Read/Grep/Glob to build context.
            """
            if !history.isEmpty {
                p += "\n\nConversation so far:\n"
                for m in history { p += "\(m.role): \(m.text)\n" }
            }
            if let sel = selection {
                p += "\n\nThe reviewer selected these diff lines:\n\(sel)\n"
            }
            p += "\nReviewer question: \(question)\nAnswer concisely and concretely."
            return p

        case let .review(pr, diffSummary):
            return """
            Review GitHub PR #\(pr.number): \(pr.title)

            PR description:
            \(pr.body)

            Changed files summary:
            \(diffSummary)

            You are inside a checkout of the PR branch. Read the changed files and review for bugs, \
            security issues, and regressions. Then output your findings as a fenced json block: \
            a JSON array of objects with keys "severity" (high|medium|low), "file", "line" (integer), \
            "explanation". Output the json block even if empty.
            """

        case let .verify(pr):
            return """
            Verify GitHub PR #\(pr.number): \(pr.title)

            The PR claims:
            \(pr.body)

            You are inside a checkout of the PR branch. Start the application following this \
            repository's own CLAUDE.md / skill instructions, then verify the claimed UI changes in \
            a real browser. Save before/after screenshot evidence as PNG files into ./difft-evidence/ \
            (create it). Finish by writing ./difft-evidence/verdict.json with keys "verdict" \
            (pass|fail|inconclusive) and "summary".
            """
        }
    }

    public var cliArguments: [String] {
        let base = ["-p", prompt, "--output-format", "stream-json", "--verbose"]
        switch self {
        case .clarify, .review:
            return base + ["--allowedTools", "Read,Grep,Glob"]
        case .verify:
            return base + ["--dangerously-skip-permissions"]
        }
    }
}
```

`Sources/DifftServices/FindingsParser.swift`:

```swift
import Foundation

public enum FindingsParser {
    public static func parse(_ finalText: String) -> [Finding] {
        let candidate: String
        if let fenceStart = finalText.range(of: "```json"),
           let fenceEnd = finalText.range(of: "```", range: fenceStart.upperBound..<finalText.endIndex) {
            candidate = String(finalText[fenceStart.upperBound..<fenceEnd.lowerBound])
        } else {
            candidate = finalText
        }
        guard let data = candidate.trimmingCharacters(in: .whitespacesAndNewlines).data(using: .utf8),
              let findings = try? JSONDecoder().decode([Finding].self, from: data) else {
            return []
        }
        return findings
    }
}
```

`Sources/DifftServices/AgentService.swift`:

```swift
import Foundation

public protocol StreamingProcessRunning: Sendable {
    func stream(_ executable: String, arguments: [String], currentDirectory: URL?) -> AsyncThrowingStream<String, Error>
    func cancel()
}

public final class DefaultStreamingProcessRunner: StreamingProcessRunning, @unchecked Sendable {
    private var process: Process?
    public init() {}

    public func stream(_ executable: String, arguments: [String], currentDirectory: URL?) -> AsyncThrowingStream<String, Error> {
        AsyncThrowingStream { cont in
            let p = Process()
            p.executableURL = URL(fileURLWithPath: "/usr/bin/env")
            p.arguments = [executable] + arguments
            p.currentDirectoryURL = currentDirectory
            let pipe = Pipe()
            p.standardOutput = pipe
            p.standardError = Pipe()
            var buffer = Data()
            pipe.fileHandleForReading.readabilityHandler = { handle in
                buffer.append(handle.availableData)
                while let nl = buffer.firstIndex(of: UInt8(ascii: "\n")) {
                    let lineData = buffer.subdata(in: buffer.startIndex..<nl)
                    buffer.removeSubrange(buffer.startIndex...nl)
                    if let line = String(data: lineData, encoding: .utf8) { cont.yield(line) }
                }
            }
            p.terminationHandler = { proc in
                pipe.fileHandleForReading.readabilityHandler = nil
                if let rest = String(data: buffer, encoding: .utf8), !rest.isEmpty { cont.yield(rest) }
                if proc.terminationStatus != 0 && proc.terminationReason == .exit {
                    cont.finish(throwing: NSError(domain: "Difft.agent", code: Int(proc.terminationStatus),
                                                  userInfo: [NSLocalizedDescriptionKey: "claude exited \(proc.terminationStatus)"]))
                } else {
                    cont.finish()
                }
            }
            do { try p.run(); self.process = p } catch { cont.finish(throwing: error) }
        }
    }

    public func cancel() { process?.terminate() }
}

public final class AgentService: @unchecked Sendable {
    private let runner: StreamingProcessRunning
    public init(runner: StreamingProcessRunning = DefaultStreamingProcessRunner()) { self.runner = runner }

    public func run(_ task: AgentTask, in worktree: URL) -> AsyncThrowingStream<AgentEvent, Error> {
        let lines = runner.stream("claude", arguments: task.cliArguments, currentDirectory: worktree)
        return AsyncThrowingStream { cont in
            Task {
                do {
                    for try await line in lines {
                        if let event = StreamJSONParser.event(fromLine: line) { cont.yield(event) }
                    }
                    cont.finish()
                } catch { cont.finish(throwing: error) }
            }
        }
    }

    public func cancel() { runner.cancel() }
}
```

- [ ] **Step 4: Run tests, verify PASS**

Run: `swift test --filter 'AgentTaskTests|FindingsParserTests|AgentServiceTests'`
Expected: all PASS.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: agent tasks, findings parser, streaming agent service"
```

---

### Task 9: WorktreeManager

**Files:**
- Create: `Sources/DifftServices/WorktreeManager.swift`
- Create: `Tests/DifftServicesTests/WorktreeManagerTests.swift`

**Interfaces:**
- Consumes: `ProcessRunning` (Task 5).
- Produces: `final class WorktreeManager { init(runner: ProcessRunning, baseDir: URL); func worktreeURL(repoName: String, prNumber: Int) -> URL; func ensureWorktree(cloneDir: URL, repoName: String, prNumber: Int) async throws -> URL; func prune(olderThan days: Int) throws }`
  - `ensureWorktree`: if dir exists → return it. Else run `gh pr checkout <N> --branch difft-pr-<N>` in cloneDir, then `git worktree add <dir> difft-pr-<N>` in cloneDir; throw `WorktreeError.commandFailed(String)` on nonzero exit.
  - `prune`: delete subdirectories of baseDir with modification date older than `days` days.

- [ ] **Step 1: Write failing tests**

`Tests/DifftServicesTests/WorktreeManagerTests.swift`:

```swift
import XCTest
@testable import DifftServices

final class WorktreeManagerTests: XCTestCase {
    var base: URL!
    override func setUp() {
        base = FileManager.default.temporaryDirectory.appendingPathComponent(UUID().uuidString)
        try? FileManager.default.createDirectory(at: base, withIntermediateDirectories: true)
    }
    override func tearDown() { try? FileManager.default.removeItem(at: base) }

    func testWorktreeURLNaming() {
        let mgr = WorktreeManager(runner: FakeProcessRunner(), baseDir: base)
        XCTAssertEqual(mgr.worktreeURL(repoName: "myrepo", prNumber: 4).lastPathComponent, "myrepo-pr4")
    }

    func testEnsureWorktreeRunsCheckoutAndWorktreeAdd() async throws {
        let fake = FakeProcessRunner()
        let mgr = WorktreeManager(runner: fake, baseDir: base)
        let clone = URL(fileURLWithPath: "/tmp/clone")
        _ = try await mgr.ensureWorktree(cloneDir: clone, repoName: "myrepo", prNumber: 4)
        XCTAssertEqual(fake.calls.count, 2)
        XCTAssertEqual(fake.calls[0].executable, "gh")
        XCTAssertEqual(fake.calls[0].arguments, ["pr", "checkout", "4", "--branch", "difft-pr-4"])
        XCTAssertEqual(fake.calls[1].executable, "git")
        XCTAssertEqual(fake.calls[1].arguments,
                       ["worktree", "add", mgr.worktreeURL(repoName: "myrepo", prNumber: 4).path, "difft-pr-4"])
    }

    func testEnsureWorktreeSkipsWhenDirExists() async throws {
        let fake = FakeProcessRunner()
        let mgr = WorktreeManager(runner: fake, baseDir: base)
        try FileManager.default.createDirectory(at: mgr.worktreeURL(repoName: "r", prNumber: 1), withIntermediateDirectories: true)
        _ = try await mgr.ensureWorktree(cloneDir: URL(fileURLWithPath: "/tmp"), repoName: "r", prNumber: 1)
        XCTAssertTrue(fake.calls.isEmpty)
    }

    func testEnsureWorktreeThrowsOnFailure() async {
        let fake = FakeProcessRunner()
        fake.responses = [ProcessResult(stdout: "", stderr: "bad pr", exitCode: 1)]
        let mgr = WorktreeManager(runner: fake, baseDir: base)
        do {
            _ = try await mgr.ensureWorktree(cloneDir: URL(fileURLWithPath: "/tmp"), repoName: "r", prNumber: 9)
            XCTFail("expected throw")
        } catch let e as WorktreeError {
            XCTAssertEqual(e, .commandFailed("bad pr"))
        } catch { XCTFail("wrong error") }
    }

    func testPruneRemovesOldDirs() throws {
        let mgr = WorktreeManager(runner: FakeProcessRunner(), baseDir: base)
        let old = base.appendingPathComponent("old-pr1")
        try FileManager.default.createDirectory(at: old, withIntermediateDirectories: true)
        try FileManager.default.setAttributes([.modificationDate: Date().addingTimeInterval(-10 * 86400)], ofItemAtPath: old.path)
        let fresh = base.appendingPathComponent("fresh-pr2")
        try FileManager.default.createDirectory(at: fresh, withIntermediateDirectories: true)
        try mgr.prune(olderThan: 7)
        XCTAssertFalse(FileManager.default.fileExists(atPath: old.path))
        XCTAssertTrue(FileManager.default.fileExists(atPath: fresh.path))
    }
}
```

- [ ] **Step 2: Run tests, verify FAIL**

Run: `swift test --filter WorktreeManagerTests`
Expected: compile error "cannot find 'WorktreeManager'".

- [ ] **Step 3: Implement**

`Sources/DifftServices/WorktreeManager.swift`:

```swift
import Foundation

public enum WorktreeError: Error, Equatable {
    case commandFailed(String)
}

public final class WorktreeManager: Sendable {
    private let runner: ProcessRunning
    private let baseDir: URL
    public init(runner: ProcessRunning, baseDir: URL) {
        self.runner = runner; self.baseDir = baseDir
        try? FileManager.default.createDirectory(at: baseDir, withIntermediateDirectories: true)
    }

    public func worktreeURL(repoName: String, prNumber: Int) -> URL {
        baseDir.appendingPathComponent("\(repoName)-pr\(prNumber)")
    }

    public func ensureWorktree(cloneDir: URL, repoName: String, prNumber: Int) async throws -> URL {
        let target = worktreeURL(repoName: repoName, prNumber: prNumber)
        if FileManager.default.fileExists(atPath: target.path) { return target }
        let branch = "difft-pr-\(prNumber)"
        let checkout = try await runner.run("gh", arguments: ["pr", "checkout", String(prNumber), "--branch", branch], currentDirectory: cloneDir)
        guard checkout.exitCode == 0 else { throw WorktreeError.commandFailed(checkout.stderr) }
        let add = try await runner.run("git", arguments: ["worktree", "add", target.path, branch], currentDirectory: cloneDir)
        guard add.exitCode == 0 else { throw WorktreeError.commandFailed(add.stderr) }
        return target
    }

    public func prune(olderThan days: Int) throws {
        let fm = FileManager.default
        let cutoff = Date().addingTimeInterval(-Double(days) * 86400)
        let contents = (try? fm.contentsOfDirectory(at: baseDir, includingPropertiesForKeys: [.contentModificationDateKey])) ?? []
        for url in contents {
            let modified = (try? url.resourceValues(forKeys: [.contentModificationDateKey]).contentModificationDate) ?? Date()
            if modified < cutoff { try fm.removeItem(at: url) }
        }
    }
}
```

- [ ] **Step 4: Run tests, verify PASS**

Run: `swift test --filter WorktreeManagerTests`
Expected: all PASS.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: worktree manager with prune"
```

---

### Task 10: ReportBuilder

**Files:**
- Create: `Sources/DifftServices/ReportBuilder.swift`
- Create: `Tests/DifftServicesTests/ReportBuilderTests.swift`

**Interfaces:**
- Consumes: `SessionData`, `Finding`, `ChatMessage` (Task 6), `FileDiff` (Task 2).
- Produces:
  - `struct ReportInput { let session: SessionData; let files: [FileDiff]; let screenshots: [(name: String, pngData: Data)] }`
  - `enum ReportBuilder { static func html(for input: ReportInput) -> String; static func defaultURL(repoName: String, prNumber: Int, date: Date) -> URL }` — self-contained HTML: PR header + verdict badge, findings table sorted high→medium→low, base64-embedded `<img>` per screenshot, Q&A transcript, per-file viewed status, collapsed (`<details>`) full diff. All user/PR text HTML-escaped.

- [ ] **Step 1: Write failing tests**

`Tests/DifftServicesTests/ReportBuilderTests.swift`:

```swift
import XCTest
@testable import DifftServices
@testable import DifftCore

final class ReportBuilderTests: XCTestCase {
    private func input() -> ReportInput {
        let pr = PullRequest(number: 3, title: "T <script>", body: "B", headRefName: "h", authorLogin: "a")
        let session = SessionData(pr: pr, repoDir: "/tmp/myrepo", viewedFiles: ["a.txt"],
            chat: [ChatMessage(role: "user", text: "q1", contextChip: "a.txt:1-2"),
                   ChatMessage(role: "assistant", text: "a1", contextChip: nil)],
            findings: [Finding(severity: "low", file: "a.txt", line: 1, explanation: "nit"),
                       Finding(severity: "high", file: "a.txt", line: 2, explanation: "bug & bad")],
            verdict: "pass")
        let files = [FileDiff(path: "a.txt", kind: .modified,
                              hunks: [Hunk(header: "@@ -1 +1 @@", lines: [DiffLine(kind: .addition, oldNumber: nil, newNumber: 1, text: "hi")])])]
        return ReportInput(session: session, files: files, screenshots: [("before.png", Data([0x89, 0x50]))])
    }

    func testContainsCoreSections() {
        let html = ReportBuilder.html(for: input())
        XCTAssertTrue(html.contains("PR #3"))
        XCTAssertTrue(html.contains("pass"))
        XCTAssertTrue(html.contains("data:image/png;base64,"))
        XCTAssertTrue(html.contains("q1"))
        XCTAssertTrue(html.contains("<details>"))
    }

    func testEscapesHTML() {
        let html = ReportBuilder.html(for: input())
        XCTAssertFalse(html.contains("<script>"))
        XCTAssertTrue(html.contains("&lt;script&gt;"))
        XCTAssertTrue(html.contains("bug &amp; bad"))
    }

    func testFindingsSortedBySeverity() {
        let html = ReportBuilder.html(for: input())
        let highPos = html.range(of: "bug &amp; bad")!.lowerBound
        let lowPos = html.range(of: "nit")!.lowerBound
        XCTAssertLessThan(highPos, lowPos)
    }

    func testDefaultURLShape() {
        let url = ReportBuilder.defaultURL(repoName: "myrepo", prNumber: 3, date: Date(timeIntervalSince1970: 0))
        XCTAssertTrue(url.path.contains("Difft-reports"))
        XCTAssertTrue(url.lastPathComponent.hasPrefix("myrepo-pr3-1970-01-01"))
        XCTAssertTrue(url.lastPathComponent.hasSuffix(".html"))
    }
}
```

- [ ] **Step 2: Run tests, verify FAIL**

Run: `swift test --filter ReportBuilderTests`
Expected: compile error "cannot find 'ReportBuilder'".

- [ ] **Step 3: Implement**

`Sources/DifftServices/ReportBuilder.swift`:

```swift
import Foundation
import DifftCore

public struct ReportInput {
    public let session: SessionData
    public let files: [FileDiff]
    public let screenshots: [(name: String, pngData: Data)]
    public init(session: SessionData, files: [FileDiff], screenshots: [(name: String, pngData: Data)]) {
        self.session = session; self.files = files; self.screenshots = screenshots
    }
}

public enum ReportBuilder {
    static func escape(_ s: String) -> String {
        s.replacingOccurrences(of: "&", with: "&amp;")
         .replacingOccurrences(of: "<", with: "&lt;")
         .replacingOccurrences(of: ">", with: "&gt;")
    }

    public static func defaultURL(repoName: String, prNumber: Int, date: Date) -> URL {
        let fmt = DateFormatter()
        fmt.dateFormat = "yyyy-MM-dd"
        fmt.timeZone = TimeZone(identifier: "UTC")
        let docs = FileManager.default.urls(for: .documentDirectory, in: .userDomainMask)[0]
        return docs.appendingPathComponent("Difft-reports/\(repoName)-pr\(prNumber)-\(fmt.string(from: date)).html")
    }

    public static func html(for input: ReportInput) -> String {
        let s = input.session
        let severityOrder = ["high": 0, "medium": 1, "low": 2]
        let findings = s.findings.sorted { (severityOrder[$0.severity] ?? 3) < (severityOrder[$1.severity] ?? 3) }

        var out = """
        <!doctype html><html><head><meta charset="utf-8"><title>Difft report — PR #\(s.pr.number)</title>
        <style>
        body { font: 14px -apple-system, sans-serif; margin: 2rem auto; max-width: 60rem; }
        .badge { padding: 2px 10px; border-radius: 10px; color: white; }
        .pass { background: #2da44e; } .fail { background: #cf222e; } .inconclusive { background: #9a6700; }
        table { border-collapse: collapse; width: 100%; }
        td, th { border: 1px solid #ddd; padding: 6px; text-align: left; vertical-align: top; }
        .sev-high { color: #cf222e; font-weight: 700; } .sev-medium { color: #9a6700; } .sev-low { color: #57606a; }
        pre { background: #f6f8fa; padding: 8px; overflow-x: auto; }
        img { max-width: 100%; border: 1px solid #ddd; }
        .add { background: #dafbe1; } .del { background: #ffebe9; }
        </style></head><body>
        <h1>PR #\(s.pr.number): \(escape(s.pr.title))</h1>
        <p>Branch <code>\(escape(s.pr.headRefName))</code> by \(escape(s.pr.authorLogin))
        """
        if let v = s.verdict {
            out += " — verdict: <span class=\"badge \(escape(v))\">\(escape(v))</span>"
        }
        out += "</p>\n<h2>Findings (\(findings.count))</h2>\n"
        if findings.isEmpty {
            out += "<p>No findings.</p>\n"
        } else {
            out += "<table><tr><th>Severity</th><th>Location</th><th>Explanation</th></tr>\n"
            for f in findings {
                out += "<tr><td class=\"sev-\(escape(f.severity))\">\(escape(f.severity))</td>"
                out += "<td><code>\(escape(f.file)):\(f.line)</code></td><td>\(escape(f.explanation))</td></tr>\n"
            }
            out += "</table>\n"
        }

        if !input.screenshots.isEmpty {
            out += "<h2>Verification evidence</h2>\n"
            for shot in input.screenshots {
                out += "<h4>\(escape(shot.name))</h4><img src=\"data:image/png;base64,\(shot.pngData.base64EncodedString())\">\n"
            }
        }

        if !s.chat.isEmpty {
            out += "<h2>Q&amp;A transcript</h2>\n"
            for m in s.chat {
                let chip = m.contextChip.map { " <code>\(escape($0))</code>" } ?? ""
                out += "<p><strong>\(escape(m.role))\(chip):</strong> \(escape(m.text))</p>\n"
            }
        }

        out += "<h2>Files (\(input.files.count))</h2>\n<ul>\n"
        for f in input.files {
            let mark = s.viewedFiles.contains(f.path) ? "✅" : "⬜️"
            out += "<li>\(mark) <code>\(escape(f.path))</code> (+\(f.additions)/−\(f.deletions))</li>\n"
        }
        out += "</ul>\n<h2>Diff</h2>\n<details><summary>Show full diff</summary>\n"
        for f in input.files {
            out += "<h4><code>\(escape(f.path))</code></h4><pre>"
            for hunk in f.hunks {
                out += escape(hunk.header) + "\n"
                for line in hunk.lines {
                    let cls = line.kind == .addition ? "add" : line.kind == .deletion ? "del" : ""
                    let sign = line.kind == .addition ? "+" : line.kind == .deletion ? "-" : " "
                    out += "<span class=\"\(cls)\">\(sign)\(escape(line.text))</span>\n"
                }
            }
            out += "</pre>\n"
        }
        out += "</details>\n</body></html>"
        return out
    }
}
```

- [ ] **Step 4: Run tests, verify PASS**

Run: `swift test --filter ReportBuilderTests`
Expected: all PASS.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: self-contained HTML report builder"
```

---

### Task 11: DiffUI — row views and file diff view

**Files:**
- Create: `Sources/DifftUI/HighlightService.swift`
- Create: `Sources/DifftUI/DiffRowView.swift`
- Create: `Sources/DifftUI/FileDiffView.swift`
- Delete: `Sources/DifftUI/Placeholder.swift`
- Create: `Tests/DifftCoreTests/UnifiedRowsTests.swift`

**Interfaces:**
- Consumes: `FileDiff`, `Hunk`, `DiffLine`, `SideBySideRow`, `RowPairer`, `IntralineDiff` (Tasks 2–4); Highlightr.
- Produces:
  - `@MainActor final class HighlightService: ObservableObject { init(); func highlighted(_ text: String, language: String?) -> AttributedString }` — Highlightr-backed, NSCache keyed by `language + "\u{1}" + text`, falls back to plain AttributedString on failure. `static func language(forPath: String) -> String?` maps extension → highlight.js language name (swift, py→python, js→javascript, ts→typescript, rb→ruby, go, rs→rust, java, kt→kotlin, css, html, json, yml/yaml→yaml, md→markdown; else nil).
  - `struct FileDiffView: View { let file: FileDiff; @Binding var layout: DiffLayout; ... }` with `enum DiffLayout: String, CaseIterable { case sideBySide, unified }`.
  - `extension RowPairer { static func unifiedRows(for hunks: [Hunk]) -> [SideBySideRow] }` in **DifftCore** (testable): unified = every line becomes a row with the line on `right` for additions/context and on `left` for deletions (renderer shows single column choosing `left ?? right`). Simpler contract: row with exactly one non-nil side except context (both same).

- [ ] **Step 1: Write failing test for unified rows (core logic only)**

`Tests/DifftCoreTests/UnifiedRowsTests.swift`:

```swift
import XCTest
@testable import DifftCore

final class UnifiedRowsTests: XCTestCase {
    func testUnifiedPreservesOrderOneRowPerLine() {
        let hunk = Hunk(header: "@@", lines: [
            DiffLine(kind: .context, oldNumber: 1, newNumber: 1, text: "c"),
            DiffLine(kind: .deletion, oldNumber: 2, newNumber: nil, text: "d"),
            DiffLine(kind: .addition, oldNumber: nil, newNumber: 2, text: "a"),
        ])
        let rows = RowPairer.unifiedRows(for: [hunk])
        XCTAssertEqual(rows.count, 3)
        XCTAssertEqual(rows[0].left?.text, "c"); XCTAssertEqual(rows[0].right?.text, "c")
        XCTAssertEqual(rows[1].left?.text, "d"); XCTAssertNil(rows[1].right)
        XCTAssertNil(rows[2].left); XCTAssertEqual(rows[2].right?.text, "a")
    }
}
```

- [ ] **Step 2: Run test, verify FAIL; implement unifiedRows in DifftCore**

Run: `swift test --filter UnifiedRowsTests` — expect compile error.

Append to `Sources/DifftCore/RowPairer.swift`:

```swift
extension RowPairer {
    public static func unifiedRows(for hunks: [Hunk]) -> [SideBySideRow] {
        var rows: [SideBySideRow] = []
        var nextID = 0
        for hunk in hunks {
            for line in hunk.lines {
                switch line.kind {
                case .context: rows.append(SideBySideRow(id: nextID, left: line, right: line))
                case .deletion: rows.append(SideBySideRow(id: nextID, left: line, right: nil))
                case .addition: rows.append(SideBySideRow(id: nextID, left: nil, right: line))
                }
                nextID += 1
            }
        }
        return rows
    }
}
```

Run: `swift test --filter UnifiedRowsTests` — expect PASS.

- [ ] **Step 3: Implement HighlightService**

`Sources/DifftUI/HighlightService.swift`:

```swift
import SwiftUI
import Highlightr

@MainActor
public final class HighlightService: ObservableObject {
    private let highlightr = Highlightr()
    private let cache = NSCache<NSString, NSAttributedString>()

    public init() {
        cache.countLimit = 20_000
        highlightr?.setTheme(to: "xcode")
    }

    public static func language(forPath path: String) -> String? {
        let map: [String: String] = [
            "swift": "swift", "py": "python", "js": "javascript", "ts": "typescript",
            "rb": "ruby", "go": "go", "rs": "rust", "java": "java", "kt": "kotlin",
            "css": "css", "html": "html", "json": "json", "yml": "yaml", "yaml": "yaml",
            "md": "markdown", "sh": "bash", "vue": "html", "c": "c", "cpp": "cpp", "h": "c",
        ]
        return map[(path as NSString).pathExtension.lowercased()]
    }

    public func highlighted(_ text: String, language: String?) -> AttributedString {
        guard let language, let highlightr else { return AttributedString(text) }
        let key = "\(language)\u{1}\(text)" as NSString
        if let cached = cache.object(forKey: key) { return AttributedString(cached) }
        guard let result = highlightr.highlight(text, as: language) else { return AttributedString(text) }
        cache.setObject(result, forKey: key)
        return AttributedString(result)
    }
}
```

- [ ] **Step 4: Implement row + file views**

`Sources/DifftUI/DiffRowView.swift`:

```swift
import SwiftUI
import DifftCore

struct HalfLineView: View {
    let line: DiffLine?
    let language: String?
    let intraline: [Range<Int>]
    @EnvironmentObject var highlighter: HighlightService

    var body: some View {
        HStack(spacing: 0) {
            Text(line.map { String($0.oldNumber ?? $0.newNumber ?? 0) } ?? "")
                .font(.system(size: 11, design: .monospaced))
                .foregroundStyle(.secondary)
                .frame(width: 44, alignment: .trailing)
                .padding(.trailing, 6)
            Text(attributed)
                .font(.system(size: 12, design: .monospaced))
                .lineLimit(1)
                .frame(maxWidth: .infinity, alignment: .leading)
        }
        .background(background)
    }

    private var attributed: AttributedString {
        guard let line else { return AttributedString("") }
        var attr = highlighter.highlighted(line.text, language: language)
        // overlay intraline emphasis
        let chars = Array(line.text)
        for range in intraline where range.upperBound <= chars.count {
            if let lo = attr.index(attr.startIndex, offsetByCharacters: range.lowerBound) as AttributedString.Index?,
               let hi = attr.index(attr.startIndex, offsetByCharacters: range.upperBound) as AttributedString.Index? {
                attr[lo..<hi].backgroundColor = line.kind == .addition
                    ? Color.green.opacity(0.45) : Color.red.opacity(0.45)
            }
        }
        return attr
    }

    private var background: Color {
        switch line?.kind {
        case .addition: Color.green.opacity(0.12)
        case .deletion: Color.red.opacity(0.12)
        case .context, nil: Color.clear
        }
    }
}

struct DiffRowView: View {
    let row: SideBySideRow
    let layout: DiffLayout
    let language: String?

    var body: some View {
        let ranges = intralineRanges
        switch layout {
        case .sideBySide:
            HStack(spacing: 1) {
                HalfLineView(line: row.left, language: language, intraline: ranges.old)
                Divider()
                HalfLineView(line: row.right, language: language, intraline: ranges.new)
            }
        case .unified:
            HalfLineView(line: row.left?.kind == .deletion ? row.left : row.right,
                         language: language,
                         intraline: row.left?.kind == .deletion ? ranges.old : ranges.new)
        }
    }

    private var intralineRanges: (old: [Range<Int>], new: [Range<Int>]) {
        guard let l = row.left, let r = row.right, l.kind == .deletion, r.kind == .addition else {
            return ([], [])
        }
        return IntralineDiff.changedRanges(old: l.text, new: r.text)
    }
}

public enum DiffLayout: String, CaseIterable, Sendable {
    case sideBySide = "Side by side"
    case unified = "Unified"
}
```

`Sources/DifftUI/FileDiffView.swift`:

```swift
import SwiftUI
import DifftCore

public struct FileDiffView: View {
    public let file: FileDiff
    @Binding public var layout: DiffLayout

    public init(file: FileDiff, layout: Binding<DiffLayout>) {
        self.file = file; self._layout = layout
    }

    public var body: some View {
        switch file.kind {
        case .binary:
            ContentUnavailableView("Binary file", systemImage: "doc.zipper",
                                   description: Text(file.path))
        default:
            List(rows) { row in
                DiffRowView(row: row, layout: layout,
                            language: HighlightService.language(forPath: file.path))
                    .listRowInsets(EdgeInsets())
                    .listRowSeparator(.hidden)
            }
            .listStyle(.plain)
            .environment(\.defaultMinListRowHeight, 18)
        }
    }

    private var rows: [SideBySideRow] {
        layout == .sideBySide ? RowPairer.rows(for: file.hunks) : RowPairer.unifiedRows(for: file.hunks)
    }
}
```

Delete `Sources/DifftUI/Placeholder.swift`.

- [ ] **Step 5: Build, run all tests, commit**

Run: `swift build && swift test`
Expected: builds clean (fix any Highlightr API drift against its 2.x README), all tests PASS.

```bash
git add -A && git commit -m "feat: pure SwiftUI diff renderer with highlighting and intraline emphasis"
```

---

### Task 12: Gutter selection

**Files:**
- Create: `Sources/DifftCore/LineSelection.swift`
- Modify: `Sources/DifftUI/DiffRowView.swift` (gutter tap/shift-tap)
- Modify: `Sources/DifftUI/FileDiffView.swift` (selection binding, copy command, tint)
- Create: `Tests/DifftCoreTests/LineSelectionTests.swift`

**Interfaces:**
- Consumes: `SideBySideRow`, `FileDiff`.
- Produces (DifftCore, testable):
  - `struct LineSelection: Equatable { var anchor: Int; var head: Int; var range: ClosedRange<Int> { min(anchor, head)...max(anchor, head) } }`
  - `enum SelectionLogic { static func click(current: LineSelection?, rowID: Int, extending: Bool) -> LineSelection; static func selectedText(rows: [SideBySideRow], selection: LineSelection) -> String; static func contextChip(path: String, rows: [SideBySideRow], selection: LineSelection) -> String }`
  - `selectedText`: for each selected row, prefer right line, prefix `+`/`-`/` ` by kind. `contextChip`: `"<path>:<firstNewOrOldNo>-<lastNewOrOldNo>"`.
- FileDiffView gains `@Binding selection: LineSelection?` and `onAsk: (String, String) -> Void` (selectedText, contextChip) invoked by floating "Ask Claude" button shown while selection non-nil.

- [ ] **Step 1: Write failing tests**

`Tests/DifftCoreTests/LineSelectionTests.swift`:

```swift
import XCTest
@testable import DifftCore

final class LineSelectionTests: XCTestCase {
    private func row(_ id: Int, kind: LineKind, old: Int?, new: Int?, _ text: String) -> SideBySideRow {
        let line = DiffLine(kind: kind, oldNumber: old, newNumber: new, text: text)
        return SideBySideRow(id: id, left: kind == .addition ? nil : line, right: kind == .deletion ? nil : line)
    }

    func testClickStartsSelection() {
        let sel = SelectionLogic.click(current: nil, rowID: 5, extending: false)
        XCTAssertEqual(sel.range, 5...5)
    }

    func testShiftClickExtends() {
        var sel = SelectionLogic.click(current: nil, rowID: 5, extending: false)
        sel = SelectionLogic.click(current: sel, rowID: 2, extending: true)
        XCTAssertEqual(sel.range, 2...5)
    }

    func testPlainClickResets() {
        var sel = SelectionLogic.click(current: LineSelection(anchor: 1, head: 9), rowID: 4, extending: false)
        XCTAssertEqual(sel.range, 4...4)
        sel = SelectionLogic.click(current: sel, rowID: 6, extending: true)
        XCTAssertEqual(sel.range, 4...6)
    }

    func testSelectedTextPrefixesSigns() {
        let rows = [
            row(0, kind: .context, old: 1, new: 1, "ctx"),
            row(1, kind: .deletion, old: 2, new: nil, "gone"),
            row(2, kind: .addition, old: nil, new: 2, "added"),
        ]
        let text = SelectionLogic.selectedText(rows: rows, selection: LineSelection(anchor: 0, head: 2))
        XCTAssertEqual(text, " ctx\n-gone\n+added")
    }

    func testContextChip() {
        let rows = [
            row(0, kind: .context, old: 10, new: 10, "a"),
            row(1, kind: .addition, old: nil, new: 11, "b"),
        ]
        let chip = SelectionLogic.contextChip(path: "src/f.swift", rows: rows, selection: LineSelection(anchor: 0, head: 1))
        XCTAssertEqual(chip, "src/f.swift:10-11")
    }
}
```

- [ ] **Step 2: Run tests, verify FAIL**

Run: `swift test --filter LineSelectionTests`
Expected: compile error "cannot find 'SelectionLogic'".

- [ ] **Step 3: Implement core logic**

`Sources/DifftCore/LineSelection.swift`:

```swift
public struct LineSelection: Equatable, Sendable {
    public var anchor: Int
    public var head: Int
    public var range: ClosedRange<Int> { min(anchor, head)...max(anchor, head) }
    public init(anchor: Int, head: Int) { self.anchor = anchor; self.head = head }
}

public enum SelectionLogic {
    public static func click(current: LineSelection?, rowID: Int, extending: Bool) -> LineSelection {
        if extending, let cur = current { return LineSelection(anchor: cur.anchor, head: rowID) }
        return LineSelection(anchor: rowID, head: rowID)
    }

    public static func selectedText(rows: [SideBySideRow], selection: LineSelection) -> String {
        rows.filter { selection.range.contains($0.id) }.compactMap { row -> String? in
            guard let line = row.right ?? row.left else { return nil }
            let sign = line.kind == .addition ? "+" : line.kind == .deletion ? "-" : " "
            return sign + line.text
        }.joined(separator: "\n")
    }

    public static func contextChip(path: String, rows: [SideBySideRow], selection: LineSelection) -> String {
        let selected = rows.filter { selection.range.contains($0.id) }
        let numbers = selected.compactMap { ($0.right ?? $0.left).flatMap { $0.newNumber ?? $0.oldNumber } }
        guard let first = numbers.first, let last = numbers.last else { return path }
        return "\(path):\(first)-\(last)"
    }
}
```

- [ ] **Step 4: Run tests, verify PASS; wire UI**

Run: `swift test --filter LineSelectionTests` — expect PASS.

In `DiffRowView`, wrap the gutter `Text` in `HalfLineView` with `.contentShape(Rectangle())` and add to `DiffRowView`:

```swift
// added properties on DiffRowView:
let isSelected: Bool
let onGutterClick: (Int, Bool) -> Void  // (rowID, shiftHeld)
```

Row body gains `.background(isSelected ? Color.accentColor.opacity(0.15) : .clear)` and the gutter gets:

```swift
.onTapGesture {
    onGutterClick(row.id, NSEvent.modifierFlags.contains(.shift))
}
```

In `FileDiffView`:

```swift
@Binding public var selection: LineSelection?
public var onAsk: (String, String) -> Void  // (selectedText, contextChip)

// in body, List row becomes:
DiffRowView(row: row, layout: layout,
            language: HighlightService.language(forPath: file.path),
            isSelected: selection.map { $0.range.contains(row.id) } ?? false,
            onGutterClick: { id, shift in
                selection = SelectionLogic.click(current: selection, rowID: id, extending: shift)
            })

// overlay on the List:
.overlay(alignment: .bottomTrailing) {
    if let sel = selection {
        Button("Ask Claude about selection") {
            let rows = self.rows
            onAsk(SelectionLogic.selectedText(rows: rows, selection: sel),
                  SelectionLogic.contextChip(path: file.path, rows: rows, selection: sel))
        }
        .buttonStyle(.borderedProminent)
        .padding()
    }
}
// copy support:
.copyable(selection.map { [SelectionLogic.selectedText(rows: rows, selection: $0)] } ?? [])
```

Run: `swift build` — expect clean build. Update the Task 11 call sites (previews/none yet) for the new parameters.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: gutter line selection with ask-claude affordance"
```

---

### Task 13: App shell — three panes, PR loading, onboarding checks

**Files:**
- Create: `Sources/DifftApp/DifftApp.swift` (replaces `main.swift` — delete it)
- Create: `Sources/DifftApp/AppModel.swift`
- Create: `Sources/DifftApp/SidebarView.swift`
- Create: `Sources/DifftApp/CenterView.swift`
- Create: `Sources/DifftApp/OnboardingView.swift`

**Interfaces:**
- Consumes: everything from Tasks 2–12.
- Produces: runnable app (`swift run DifftApp`); `@MainActor final class AppModel: ObservableObject` — the app-level coordinator:
  - `@Published var repoDir: URL?` (user picks clone via `NSOpenPanel`; persisted in `UserDefaults` key `"repoDir"`)
  - `@Published var prs: [PullRequest]`, `@Published var session: ReviewSession?`, `@Published var files: [FileDiff]`
  - `@Published var toolCheck: (gh: Bool, ghAuth: Bool, claude: Bool)?`
  - `func checkTools() async`, `func loadPRs() async`, `func openPR(_ pr: PullRequest) async` (fetch diff, load-or-create SessionData via SessionStore, wrap in ReviewSession)
  - `func markViewed(_ path: String, viewed: Bool)` (updates session + saves)
  - `let sessionStore = SessionStore(directory: <Application Support/Difft/sessions>)`

- [ ] **Step 1: Implement AppModel**

`Sources/DifftApp/AppModel.swift`:

```swift
import SwiftUI
import DifftCore
import DifftServices

@MainActor
final class AppModel: ObservableObject {
    @Published var repoDir: URL? {
        didSet { UserDefaults.standard.set(repoDir?.path, forKey: "repoDir") }
    }
    @Published var prs: [PullRequest] = []
    @Published var session: ReviewSession?
    @Published var files: [FileDiff] = []
    @Published var toolCheck: (gh: Bool, ghAuth: Bool, claude: Bool)?
    @Published var errorBanner: String?

    let github = GitHubService()
    let sessionStore: SessionStore
    let processRunner = DefaultProcessRunner()

    static var appSupportDir: URL {
        FileManager.default.urls(for: .applicationSupportDirectory, in: .userDomainMask)[0]
            .appendingPathComponent("Difft")
    }

    init() {
        sessionStore = SessionStore(directory: Self.appSupportDir.appendingPathComponent("sessions"))
        if let path = UserDefaults.standard.string(forKey: "repoDir") {
            repoDir = URL(fileURLWithPath: path)
        }
    }

    var repoName: String { repoDir?.lastPathComponent ?? "" }

    func checkTools() async {
        let gh = await github.checkAvailability()
        let claude = (try? await processRunner.run("which", arguments: ["claude"], currentDirectory: nil))?.exitCode == 0
        toolCheck = (gh.ghInstalled, gh.ghAuthed, claude)
    }

    func loadPRs() async {
        guard let repoDir else { return }
        do { prs = try await github.listPRs(repoDir: repoDir); errorBanner = nil }
        catch { errorBanner = "Failed to list PRs: \(error.localizedDescription)" }
    }

    func openPR(_ pr: PullRequest) async {
        guard let repoDir else { return }
        do {
            files = try await github.fetchDiff(repoDir: repoDir, number: pr.number)
            let data = sessionStore.load(repo: repoName, prNumber: pr.number)
                ?? SessionData(pr: pr, repoDir: repoDir.path, viewedFiles: [], chat: [], findings: [], verdict: nil)
            session = ReviewSession(data: data)
            session?.selectedFile = files.first?.path
            errorBanner = nil
        } catch { errorBanner = "Failed to open PR #\(pr.number): \(error.localizedDescription)" }
    }

    func markViewed(_ path: String, viewed: Bool) {
        guard let session else { return }
        if viewed { session.data.viewedFiles.insert(path) } else { session.data.viewedFiles.remove(path) }
        try? sessionStore.save(session.data)
    }
}
```

- [ ] **Step 2: Implement app entry + onboarding**

Delete `Sources/DifftApp/main.swift`. `Sources/DifftApp/DifftApp.swift`:

```swift
import SwiftUI
import DifftUI

@main
struct DifftApp: App {
    @StateObject private var model = AppModel()
    @StateObject private var highlighter = HighlightService()

    init() {
        NSApplication.shared.setActivationPolicy(.regular) // needed when run via `swift run`
    }

    var body: some Scene {
        WindowGroup("Difft") {
            RootView()
                .environmentObject(model)
                .environmentObject(highlighter)
                .task { await model.checkTools() }
                .frame(minWidth: 1100, minHeight: 700)
        }
    }
}

struct RootView: View {
    @EnvironmentObject var model: AppModel

    var body: some View {
        if let check = model.toolCheck, !(check.gh && check.ghAuth && check.claude) {
            OnboardingView(check: check)
        } else {
            MainSplitView()
        }
    }
}
```

`Sources/DifftApp/OnboardingView.swift`:

```swift
import SwiftUI

struct OnboardingView: View {
    @EnvironmentObject var model: AppModel
    let check: (gh: Bool, ghAuth: Bool, claude: Bool)

    var body: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("Difft needs these tools").font(.title2)
            row(ok: check.gh, label: "GitHub CLI installed", fix: "brew install gh")
            row(ok: check.ghAuth, label: "GitHub CLI authenticated", fix: "gh auth login")
            row(ok: check.claude, label: "Claude Code installed", fix: "https://docs.anthropic.com/claude-code")
            Button("Re-check") { Task { await model.checkTools() } }
        }
        .padding(40)
    }

    private func row(ok: Bool, label: String, fix: String) -> some View {
        HStack {
            Image(systemName: ok ? "checkmark.circle.fill" : "xmark.circle.fill")
                .foregroundStyle(ok ? .green : .red)
            Text(label)
            if !ok { Text(fix).font(.caption.monospaced()).textSelection(.enabled) }
        }
    }
}
```

- [ ] **Step 3: Implement sidebar + center + split**

`Sources/DifftApp/SidebarView.swift`:

```swift
import SwiftUI
import DifftServices

struct SidebarView: View {
    @EnvironmentObject var model: AppModel

    var body: some View {
        VStack(spacing: 0) {
            HStack {
                Text(model.repoName.isEmpty ? "No repo" : model.repoName).bold()
                Spacer()
                Button("Choose…") {
                    let panel = NSOpenPanel()
                    panel.canChooseDirectories = true; panel.canChooseFiles = false
                    if panel.runModal() == .OK, let url = panel.url {
                        model.repoDir = url
                        Task { await model.loadPRs() }
                    }
                }
            }.padding(8)

            if model.session == nil {
                List(model.prs) { pr in
                    VStack(alignment: .leading) {
                        Text("#\(pr.number) \(pr.title)").lineLimit(2)
                        Text(pr.authorLogin).font(.caption).foregroundStyle(.secondary)
                    }
                    .contentShape(Rectangle())
                    .onTapGesture { Task { await model.openPR(pr) } }
                }
                .overlay { if model.prs.isEmpty { Text("No open PRs").foregroundStyle(.secondary) } }
                .task { await model.loadPRs() }
            } else {
                fileTree
            }
        }
    }

    @ViewBuilder private var fileTree: some View {
        if let session = model.session {
            let viewed = session.data.viewedFiles
            VStack(spacing: 0) {
                HStack {
                    Button("← PRs") { model.session = nil; model.files = [] }
                    Spacer()
                    Text("\(viewed.count)/\(model.files.count) viewed").font(.caption)
                }.padding(6)
                ProgressView(value: Double(viewed.count), total: Double(max(model.files.count, 1)))
                    .padding(.horizontal, 6)
                List(model.files, selection: Binding(
                    get: { session.selectedFile },
                    set: { session.selectedFile = $0 })) { file in
                    HStack {
                        Toggle("", isOn: Binding(
                            get: { viewed.contains(file.path) },
                            set: { model.markViewed(file.path, viewed: $0) }))
                            .labelsHidden().toggleStyle(.checkbox)
                        Text(file.path).lineLimit(1).truncationMode(.head)
                        Spacer()
                        Text("+\(file.additions)").foregroundStyle(.green).font(.caption)
                        Text("−\(file.deletions)").foregroundStyle(.red).font(.caption)
                    }
                    .tag(file.path)
                }
            }
        }
    }
}
```

`Sources/DifftApp/CenterView.swift`:

```swift
import SwiftUI
import DifftCore
import DifftUI

struct CenterView: View {
    @EnvironmentObject var model: AppModel
    @State private var layout: DiffLayout = .sideBySide
    @State private var selection: LineSelection?
    var onAsk: (String, String) -> Void

    var body: some View {
        if let session = model.session,
           let path = session.selectedFile,
           let file = model.files.first(where: { $0.path == path }) {
            FileDiffView(file: file, layout: $layout, selection: $selection, onAsk: onAsk)
                .id(file.path) // reset scroll + selection per file
                .onChange(of: file.path) { selection = nil }
                .toolbar {
                    Picker("Layout", selection: $layout) {
                        ForEach(DiffLayout.allCases, id: \.self) { Text($0.rawValue) }
                    }.pickerStyle(.segmented)
                }
        } else {
            ContentUnavailableView("Pick a PR", systemImage: "arrow.triangle.pull")
        }
    }
}
```

Add j/k next/prev-file keys and a font-size control. In `CenterView`, on the `FileDiffView`:

```swift
.focusable()
.onKeyPress("j") { step(1); return .handled }
.onKeyPress("k") { step(-1); return .handled }
.toolbar {
    Stepper("Font \(fontSize)pt", value: $fontSize, in: 9...18)
}
```

with helpers on `CenterView`:

```swift
@AppStorage("diffFontSize") private var fontSize = 12

private func step(_ delta: Int) {
    guard let session = model.session,
          let current = session.selectedFile,
          let idx = model.files.firstIndex(where: { $0.path == current }) else { return }
    let next = idx + delta
    if model.files.indices.contains(next) { session.selectedFile = model.files[next].path }
}
```

Thread `fontSize` through `FileDiffView` → `DiffRowView` → `HalfLineView` as a plain `let fontSize: Int` parameter; `HalfLineView` uses `.font(.system(size: CGFloat(fontSize), design: .monospaced))` for code text and `size: CGFloat(fontSize) - 1` for the gutter.

```swift
// (end of CenterView.swift additions)

struct MainSplitView: View {
    @EnvironmentObject var model: AppModel
    @State private var pendingAsk: (text: String, chip: String)?

    var body: some View {
        NavigationSplitView {
            SidebarView().navigationSplitViewColumnWidth(min: 220, ideal: 280)
        } content: {
            VStack(spacing: 0) {
                if let banner = model.errorBanner {
                    Text(banner).foregroundStyle(.white).padding(6)
                        .frame(maxWidth: .infinity).background(.red)
                }
                CenterView { text, chip in pendingAsk = (text, chip) }
                StatusBar()
            }
        } detail: {
            RightPanel(pendingAsk: $pendingAsk)
                .navigationSplitViewColumnWidth(min: 300, ideal: 380)
        }
    }
}

struct StatusBar: View {
    @EnvironmentObject var model: AppModel

    var body: some View {
        HStack {
            if let s = model.session {
                Text("#\(s.data.pr.number) \(s.data.pr.title)").lineLimit(1)
                Text(s.data.pr.headRefName).font(.caption.monospaced()).foregroundStyle(.secondary)
            }
            Spacer()
            AgentStatusView()
        }
        .padding(.horizontal, 8).padding(.vertical, 4)
        .background(.bar)
    }
}
```

`AgentStatusView` and `RightPanel` are Task 14 — for THIS task create stubs in `CenterView.swift` so it builds:

```swift
struct AgentStatusView: View {
    var body: some View { EmptyView() }
}

struct RightPanel: View {
    @Binding var pendingAsk: (text: String, chip: String)?
    var body: some View { Text("Panel — Task 14").frame(maxWidth: .infinity, maxHeight: .infinity) }
}
```

- [ ] **Step 4: Build, run, verify manually**

Run: `swift build && swift test` — clean build, all tests still PASS.
Run: `swift run DifftApp` against a real repo clone with open PRs:
- onboarding appears if tools missing; otherwise pick repo → PR list loads → click PR → file tree + diff render, layout toggle works, checkboxes persist after app restart (relaunch to confirm).

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: app shell with sidebar, diff center pane, onboarding"
```

---

### Task 14: Right panel — Claude chat + Findings + review run

**Files:**
- Create: `Sources/DifftApp/RightPanel.swift` (replace stub in CenterView.swift — delete stub structs there)
- Create: `Sources/DifftApp/AgentController.swift`

**Interfaces:**
- Consumes: `AgentService`, `AgentTask`, `AgentEvent`, `FindingsParser`, `WorktreeManager`, `ReviewSession` (Tasks 6–9), `pendingAsk` binding (Task 13).
- Produces:
  - `@MainActor final class AgentController: ObservableObject` — `init(model: AppModel)`; `@Published var streamingText: String`; `@Published var toolActivity: [String]`; `func ask(question: String, selection: (text: String, chip: String)?) async`; `func runReview() async`; `func runVerify() async` (Task 15 fills body; stub now sets `agentState = .failed("not implemented")`); `func cancel()`. Serializes: guard `session.agentState == .idle` at entry. On result event: append assistant ChatMessage (ask) or parse findings into session (review); save session; set `.idle` / `.failed`.
  - `RightPanel` with tabs Claude / Findings / Verify; `AgentStatusView` shows spinner + task name + Cancel when running.

- [ ] **Step 1: Implement AgentController**

`Sources/DifftApp/AgentController.swift`:

```swift
import SwiftUI
import DifftCore
import DifftServices

@MainActor
final class AgentController: ObservableObject {
    @Published var streamingText = ""
    @Published var toolActivity: [String] = []

    private let model: AppModel
    private let agent = AgentService()
    private lazy var worktrees = WorktreeManager(
        runner: model.processRunner,
        baseDir: AppModel.appSupportDir.appendingPathComponent("worktrees"))

    init(model: AppModel) { self.model = model }

    private func withWorktree(_ label: String, _ body: (URL) async throws -> Void) async {
        guard let session = model.session, session.agentState == .idle, let repoDir = model.repoDir else { return }
        session.agentState = .running(label)
        streamingText = ""; toolActivity = []
        do {
            let wt = try await worktrees.ensureWorktree(
                cloneDir: repoDir, repoName: model.repoName, prNumber: session.data.pr.number)
            try await body(wt)
            session.agentState = .idle
        } catch {
            session.agentState = .failed("\(label): \(error.localizedDescription)")
        }
        try? model.sessionStore.save(session.data)
    }

    func ask(question: String, selection: (text: String, chip: String)?) async {
        guard let session = model.session else { return }
        session.data.chat.append(ChatMessage(role: "user", text: question, contextChip: selection?.chip))
        let task = AgentTask.clarify(
            pr: session.data.pr,
            selection: selection.map { "\($0.chip)\n\($0.text)" },
            question: question,
            history: session.data.chat.dropLast().suffix(10).map { $0 })
        await withWorktree("Clarifying") { wt in
            var final = ""
            for try await event in agent.run(task, in: wt) {
                self.consume(event, accumulatingResult: &final)
            }
            session.data.chat.append(ChatMessage(role: "assistant",
                text: final.isEmpty ? self.streamingText : final, contextChip: nil))
        }
    }

    func runReview() async {
        guard let session = model.session else { return }
        let summary = model.files.map { "\($0.path) (+\($0.additions)/−\($0.deletions))" }.joined(separator: "\n")
        let task = AgentTask.review(pr: session.data.pr, diffSummary: summary)
        await withWorktree("Reviewing") { wt in
            var final = ""
            for try await event in agent.run(task, in: wt) {
                self.consume(event, accumulatingResult: &final)
            }
            session.data.findings = FindingsParser.parse(final)
        }
    }

    func runVerify() async {
        model.session?.agentState = .failed("Verify: implemented in Task 15")
    }

    func cancel() {
        agent.cancel()
        model.session?.agentState = .idle
    }

    private func consume(_ event: AgentEvent, accumulatingResult final: inout String) {
        switch event {
        case .textDelta(let t): streamingText += t
        case .toolUse(let name): toolActivity.append(name)
        case .result(let isError, let text):
            final = text
            if isError { model.session?.agentState = .failed(text) }
        case .unknown: break
        }
    }
}
```

- [ ] **Step 2: Implement RightPanel + AgentStatusView**

Delete the `RightPanel`/`AgentStatusView` stubs from `CenterView.swift`. `Sources/DifftApp/RightPanel.swift`:

```swift
import SwiftUI
import DifftServices

struct RightPanel: View {
    @EnvironmentObject var model: AppModel
    @Binding var pendingAsk: (text: String, chip: String)?
    @StateObject private var controllerBox = ControllerBox()
    @State private var tab = 0
    @State private var question = ""

    var body: some View {
        let controller = controllerBox.controller(for: model)
        VStack(spacing: 0) {
            Picker("", selection: $tab) {
                Text("Claude").tag(0); Text("Findings").tag(1); Text("Verify").tag(2)
            }.pickerStyle(.segmented).padding(6)

            switch tab {
            case 0: chatTab(controller)
            case 1: findingsTab(controller)
            default: VerifyTab(controller: controller)   // Task 15
            }
        }
        .onChange(of: pendingAsk?.chip) { if pendingAsk != nil { tab = 0 } }
    }

    @ViewBuilder private func chatTab(_ controller: AgentController) -> some View {
        ScrollView {
            LazyVStack(alignment: .leading, spacing: 8) {
                ForEach(Array((model.session?.data.chat ?? []).enumerated()), id: \.offset) { _, msg in
                    VStack(alignment: .leading, spacing: 2) {
                        if let chip = msg.contextChip {
                            Text(chip).font(.caption.monospaced())
                                .padding(3).background(.quaternary, in: RoundedRectangle(cornerRadius: 4))
                        }
                        Text(msg.text)
                            .padding(8)
                            .background(msg.role == "user" ? Color.accentColor.opacity(0.15) : Color.gray.opacity(0.1),
                                        in: RoundedRectangle(cornerRadius: 8))
                    }
                    .frame(maxWidth: .infinity, alignment: msg.role == "user" ? .trailing : .leading)
                }
                if case .running = model.session?.agentState, !controller.streamingText.isEmpty {
                    Text(controller.streamingText).padding(8).foregroundStyle(.secondary)
                }
            }.padding(8)
        }
        if let chip = pendingAsk?.chip {
            HStack {
                Text(chip).font(.caption.monospaced())
                Button("✕") { pendingAsk = nil }.buttonStyle(.plain)
            }.padding(.horizontal, 8)
        }
        HStack {
            TextField("Ask about this PR…", text: $question).onSubmit { submit(controller) }
            Button("Ask") { submit(controller) }
                .disabled(question.isEmpty || model.session?.agentState != .idle)
        }.padding(8)
    }

    private func submit(_ controller: AgentController) {
        let q = question, sel = pendingAsk
        question = ""; pendingAsk = nil
        Task { await controller.ask(question: q, selection: sel) }
    }

    @ViewBuilder private func findingsTab(_ controller: AgentController) -> some View {
        VStack {
            Button("Run Claude review") { Task { await controller.runReview() } }
                .disabled(model.session?.agentState != .idle)
                .padding(.top, 8)
            List(Array((model.session?.data.findings ?? []).enumerated()), id: \.offset) { _, f in
                VStack(alignment: .leading) {
                    HStack {
                        Text(f.severity.uppercased()).font(.caption.bold())
                            .foregroundStyle(f.severity == "high" ? .red : f.severity == "medium" ? .orange : .secondary)
                        Text("\(f.file):\(f.line)").font(.caption.monospaced())
                    }
                    Text(f.explanation)
                }
                .contentShape(Rectangle())
                .onTapGesture { model.session?.selectedFile = f.file }
            }
        }
    }
}

// AgentController depends on AppModel from the environment, so build it lazily once.
@MainActor
final class ControllerBox: ObservableObject {
    private var stored: AgentController?
    func controller(for model: AppModel) -> AgentController {
        if let stored { return stored }
        let c = AgentController(model: model); stored = c; return c
    }
}

struct AgentStatusView: View {
    @EnvironmentObject var model: AppModel

    var body: some View {
        switch model.session?.agentState {
        case .running(let label):
            HStack(spacing: 6) {
                ProgressView().controlSize(.small)
                Text(label)
                Button("Cancel") { NotificationCenter.default.post(name: .difftCancelAgent, object: nil) }
            }
        case .failed(let msg):
            Text(msg).foregroundStyle(.red).lineLimit(1).help(msg)
        default: EmptyView()
        }
    }
}

extension Notification.Name { static let difftCancelAgent = Notification.Name("difftCancelAgent") }
```

Wire cancel: in `RightPanel.body` add
`.onReceive(NotificationCenter.default.publisher(for: .difftCancelAgent)) { _ in controllerBox.controller(for: model).cancel() }`.
Add a placeholder `VerifyTab` (filled in Task 15) at the bottom of the file:

```swift
struct VerifyTab: View {
    let controller: AgentController
    var body: some View { Text("Verify — Task 15").frame(maxWidth: .infinity, maxHeight: .infinity) }
}
```

- [ ] **Step 3: Build + test**

Run: `swift build && swift test`
Expected: clean build, all tests PASS.

- [ ] **Step 4: Manual verification**

Run: `swift run DifftApp` with a real repo + real PR:
- Select lines → Ask Claude → chip appears in panel, ask a question → streaming text appears → final answer lands in transcript → persists after relaunch.
- Findings tab → Run Claude review → findings list populates, click jumps file selection.
- Cancel button stops a running task, state returns to idle.

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: claude chat and review findings panel"
```

---

### Task 15: Verify flow — confirm dialog, evidence watcher, verdict

**Files:**
- Modify: `Sources/DifftApp/AgentController.swift` (real `runVerify`)
- Modify: `Sources/DifftApp/RightPanel.swift` (real `VerifyTab`)
- Create: `Sources/DifftServices/EvidenceWatcher.swift`
- Create: `Tests/DifftServicesTests/EvidenceWatcherTests.swift`

**Interfaces:**
- Consumes: `AgentTask.verify` (Task 8), `WorktreeManager` (Task 9), session verdict field (Task 6).
- Produces:
  - `final class EvidenceWatcher { init(directory: URL); var onChange: (@Sendable ([URL]) -> Void)?; func start(); func stop(); static func currentPNGs(in directory: URL) -> [URL]; static func verdict(in directory: URL) -> (verdict: String, summary: String)? }` — polls the directory every 2s with a `Timer` for `*.png` (sorted by name) and parses `verdict.json` (`{"verdict": "...", "summary": "..."}`).
  - `runVerify()` real body: gated on `verifyConfirmed` parameter — the UI shows the dialog, controller never skips it.

- [ ] **Step 1: Write failing EvidenceWatcher tests**

`Tests/DifftServicesTests/EvidenceWatcherTests.swift`:

```swift
import XCTest
@testable import DifftServices

final class EvidenceWatcherTests: XCTestCase {
    var dir: URL!
    override func setUp() {
        dir = FileManager.default.temporaryDirectory.appendingPathComponent(UUID().uuidString)
        try? FileManager.default.createDirectory(at: dir, withIntermediateDirectories: true)
    }
    override func tearDown() { try? FileManager.default.removeItem(at: dir) }

    func testCurrentPNGsSortedAndFiltered() throws {
        try Data().write(to: dir.appendingPathComponent("b.png"))
        try Data().write(to: dir.appendingPathComponent("a.png"))
        try Data().write(to: dir.appendingPathComponent("notes.txt"))
        XCTAssertEqual(EvidenceWatcher.currentPNGs(in: dir).map(\.lastPathComponent), ["a.png", "b.png"])
    }

    func testMissingDirEmpty() {
        XCTAssertEqual(EvidenceWatcher.currentPNGs(in: dir.appendingPathComponent("nope")), [])
    }

    func testVerdictParsing() throws {
        try Data(#"{"verdict": "pass", "summary": "Looks right"}"#.utf8)
            .write(to: dir.appendingPathComponent("verdict.json"))
        let v = EvidenceWatcher.verdict(in: dir)
        XCTAssertEqual(v?.verdict, "pass")
        XCTAssertEqual(v?.summary, "Looks right")
    }

    func testVerdictNilWhenMissingOrBroken() throws {
        XCTAssertNil(EvidenceWatcher.verdict(in: dir))
        try Data("broken".utf8).write(to: dir.appendingPathComponent("verdict.json"))
        XCTAssertNil(EvidenceWatcher.verdict(in: dir))
    }
}
```

- [ ] **Step 2: Run tests, verify FAIL; implement EvidenceWatcher**

Run: `swift test --filter EvidenceWatcherTests` — compile error expected.

`Sources/DifftServices/EvidenceWatcher.swift`:

```swift
import Foundation

public final class EvidenceWatcher: @unchecked Sendable {
    private let directory: URL
    private var timer: Timer?
    public var onChange: (@Sendable ([URL]) -> Void)?

    public init(directory: URL) { self.directory = directory }

    public static func currentPNGs(in directory: URL) -> [URL] {
        let urls = (try? FileManager.default.contentsOfDirectory(at: directory, includingPropertiesForKeys: nil)) ?? []
        return urls.filter { $0.pathExtension.lowercased() == "png" }
                   .sorted { $0.lastPathComponent < $1.lastPathComponent }
    }

    public static func verdict(in directory: URL) -> (verdict: String, summary: String)? {
        struct V: Codable { let verdict: String; let summary: String }
        guard let data = try? Data(contentsOf: directory.appendingPathComponent("verdict.json")),
              let v = try? JSONDecoder().decode(V.self, from: data) else { return nil }
        return (v.verdict, v.summary)
    }

    public func start() {
        stop()
        let dir = directory
        var lastNames: [String] = []
        let t = Timer(timeInterval: 2, repeats: true) { [weak self] _ in
            let pngs = Self.currentPNGs(in: dir)
            let names = pngs.map(\.lastPathComponent)
            if names != lastNames { lastNames = names; self?.onChange?(pngs) }
        }
        RunLoop.main.add(t, forMode: .common)
        timer = t
    }

    public func stop() { timer?.invalidate(); timer = nil }
}
```

Run: `swift test --filter EvidenceWatcherTests` — expect PASS.

- [ ] **Step 3: Real runVerify in AgentController**

Replace the stub:

```swift
@Published var evidence: [URL] = []
private var watcher: EvidenceWatcher?

func runVerify() async {
    guard let session = model.session else { return }
    let task = AgentTask.verify(pr: session.data.pr)
    await withWorktree("Verifying") { wt in
        let evidenceDir = wt.appendingPathComponent("difft-evidence")
        let w = EvidenceWatcher(directory: evidenceDir)
        w.onChange = { urls in Task { @MainActor in self.evidence = urls } }
        w.start(); self.watcher = w
        defer { w.stop(); self.watcher = nil }

        var final = ""
        for try await event in agent.run(task, in: wt) {
            self.consume(event, accumulatingResult: &final)
        }
        self.evidence = EvidenceWatcher.currentPNGs(in: evidenceDir)
        if let v = EvidenceWatcher.verdict(in: evidenceDir) {
            session.data.verdict = v.verdict
            session.data.chat.append(ChatMessage(role: "assistant",
                text: "Verification \(v.verdict): \(v.summary)", contextChip: nil))
        } else {
            session.data.verdict = "inconclusive"
        }
    }
}
```

- [ ] **Step 4: Real VerifyTab with mandatory confirm dialog**

Replace the stub in `RightPanel.swift`:

```swift
struct VerifyTab: View {
    @EnvironmentObject var model: AppModel
    @ObservedObject var controller: AgentController
    @State private var confirmShown = false

    var body: some View {
        VStack(spacing: 8) {
            Button("Verify UI changes") { confirmShown = true }
                .disabled(model.session?.agentState != .idle)
                .confirmationDialog(
                    "Run PR code with full permissions?",
                    isPresented: $confirmShown, titleVisibility: .visible) {
                    Button("Run verification", role: .destructive) {
                        Task { await controller.runVerify() }
                    }
                    Button("Cancel", role: .cancel) {}
                } message: {
                    Text("Claude will execute this PR's code with --dangerously-skip-permissions inside the worktree. Only proceed if you trust this PR's author.")
                }

            if let verdict = model.session?.data.verdict {
                Text("Verdict: \(verdict)").font(.headline)
                    .foregroundStyle(verdict == "pass" ? .green : verdict == "fail" ? .red : .orange)
            }

            ScrollView {
                VStack(alignment: .leading, spacing: 4) {
                    ForEach(controller.toolActivity.indices, id: \.self) { i in
                        Text("🔧 \(controller.toolActivity[i])").font(.caption.monospaced())
                    }
                    LazyVGrid(columns: [GridItem(.adaptive(minimum: 150))]) {
                        ForEach(controller.evidence, id: \.self) { url in
                            if let img = NSImage(contentsOf: url) {
                                Image(nsImage: img).resizable().scaledToFit()
                                    .onTapGesture { NSWorkspace.shared.open(url) }
                            }
                        }
                    }
                }.padding(8)
            }
        }
        .padding(.top, 8)
    }
}
```

Update the `RightPanel` tab switch to pass `controller` as `@ObservedObject` (`VerifyTab(controller: controller)` already matches).

- [ ] **Step 5: Build, test, manual check, commit**

Run: `swift build && swift test` — clean, all PASS.
Manual: on a small real PR with UI changes — Verify tab → confirm dialog appears EVERY run → agent runs → tool activity + screenshots appear live → verdict badge shown, persists after relaunch.

```bash
git add -A && git commit -m "feat: browser verification flow with confirm gate and evidence watcher"
```

---

### Task 16: Report generation + wiring + perf pass

**Files:**
- Modify: `Sources/DifftApp/CenterView.swift` (StatusBar gains Report button)
- Modify: `Sources/DifftApp/AppModel.swift` (`func generateReport(evidence: [URL]) throws -> URL`)

**Interfaces:**
- Consumes: `ReportBuilder`, `ReportInput` (Task 10), evidence URLs (Task 15).
- Produces: report file on disk, opened in default browser.

- [ ] **Step 1: Implement generateReport**

Add to `AppModel`:

```swift
func generateReport(evidence: [URL]) throws -> URL {
    guard let session else { throw GitHubServiceError.commandFailed("no session") }
    let screenshots: [(name: String, pngData: Data)] = evidence.compactMap { url in
        (try? Data(contentsOf: url)).map { (url.lastPathComponent, $0) }
    }
    let html = ReportBuilder.html(for: ReportInput(session: session.data, files: files, screenshots: screenshots))
    let url = ReportBuilder.defaultURL(repoName: repoName, prNumber: session.data.pr.number, date: Date())
    try FileManager.default.createDirectory(at: url.deletingLastPathComponent(), withIntermediateDirectories: true)
    try Data(html.utf8).write(to: url)
    return url
}
```

- [ ] **Step 2: Wire Report button**

In `StatusBar`, after `AgentStatusView()`:

```swift
Button("Report") {
    let evidence = (try? model.session.map { _ in () }) != nil ? currentEvidence() : []
    if let url = try? model.generateReport(evidence: evidence) {
        NSWorkspace.shared.open(url)
    }
}
.disabled(model.session == nil)
```

`currentEvidence()` helper on StatusBar reads from the worktree evidence dir:

```swift
private func currentEvidence() -> [URL] {
    guard let s = model.session else { return [] }
    let dir = AppModel.appSupportDir
        .appendingPathComponent("worktrees/\(model.repoName)-pr\(s.data.pr.number)/difft-evidence")
    return EvidenceWatcher.currentPNGs(in: dir)
}
```

(Import `DifftServices` in the file if not present.)

- [ ] **Step 3: Build + full test suite**

Run: `swift build && swift test`
Expected: clean, all PASS.

- [ ] **Step 4: Manual end-to-end + perf check**

- Full flow on a real PR: open → review files → ask question → run review → verify → Report button → HTML opens in browser with all sections populated.
- Perf: open a large PR (300+ files or 10k+ lines; e.g. a big dependency-bump PR on a public repo). File list must render instantly; switching files must not beachball. If a single huge file stutters, confirm rows render via `List` laziness before optimizing further.
- Worktree prune: call `WorktreeManager.prune(olderThan: 7)` in `AppModel.init`'s `Task {}` — add it now:

```swift
Task {
    try? WorktreeManager(runner: DefaultProcessRunner(),
                         baseDir: Self.appSupportDir.appendingPathComponent("worktrees"))
        .prune(olderThan: 7)
}
```

- Manual worktree cleanup button (spec requirement): add to `StatusBar`, next to Report:

```swift
Button("Clean worktrees") {
    try? WorktreeManager(runner: model.processRunner,
                         baseDir: AppModel.appSupportDir.appendingPathComponent("worktrees"))
        .prune(olderThan: 0)
}
.help("Delete all Difft worktree checkouts")
```

- [ ] **Step 5: Final commit**

```bash
git add -A && git commit -m "feat: report generation and worktree prune; v1 complete"
```

---

## Post-plan notes for the executor

- `swift run DifftApp` is the dev loop; no .app bundle in v1 scope (run from terminal).
- If Highlightr's API differs from `highlight(_:as:)`, check its README at the pinned 2.x version and adapt `HighlightService` only.
- If `claude` stream-json shapes differ in a newer CLI version, adjust `StreamJSONParser` and its test fixtures together, using real captured output (`claude -p "say hi" --output-format stream-json --verbose > fixture.txt`).
- Known v1 limitation (documented in spec): per-line syntax highlighting loses multi-line token state (block comments render as code).
